# 99 — Open Questions (cross-cutting)

Формат: `Q-NNN-N`. Дефолты применены там, где это не блокирует реализацию backend. Блокеры эскалированы в orchestrator (`blocking_questions`).

| ID | Вопрос | Статус | Принятый дефолт (если есть) | Блокирует backend? |
|---|---|---|---|---|
| Q-001-1 | TTL/expiry `sessionId`: сколько живёт сессия, что после истечения? | Open | Soft TTL **24h** по `updated_at`; продолжение истёкшей сессии → новая сессия, старый контекст не догружается. Реализуемо на дефолте. | Нет |
| Q-002-1 | Конкретный KMS-эквивалент для envelope encryption BYOK (AWS KMS / GCP KMS / Azure Key Vault / HashiCorp Vault / Transit). | **Open — отложен (post-MVP)** | **На MVP — `LocalKmsClient`** (реальный AES-256-GCM wrap DEK под `KMS_LOCAL_MASTER_KEY` из secret manager/env, [ADR-003](adr/ADR-003-byok-envelope-encryption.md), [05-security.md](05-security.md#byok--шифрование-at-rest-envelope-encryption)). Миграция на облачный KMS — post-MVP, тот же интерфейс `KmsClient`. Отвязан от deploy-target ([TD-005](100-known-tech-debt.md) закрыт независимо). | Нет (MVP работает на LocalKmsClient) |
| Q-003-1 | Точные значения rate limits per user/device/IP. | Open | chat/run: 30/60/120 req/min (user/device/IP); прочие POST: 60 req/min/user. Конфигурируемо через env. | Нет |
| Q-003-2 | Точные size-лимиты payload/context/tool-result. | Open | message ≤32KB, context ≤64KB, tool-result ≤256KB, body ≤512KB. Конфигурируемо. | Нет |
| Q-004-1 | **Формула конвертации usage (input/output tokens) → кредиты.** Тарифные коэффициенты на модель. | **Closed** ([ADR-006](adr/ADR-006-credit-billing-and-subscription-grant.md)) | **1 кредит = 1 сообщение**: завершённый message-шаг (assistant_message) списывает `amount=1`; tool-раунды не списывают (1 списание на message-шаг, идемпотентно по `messageStepId` — не по gateway `requestId`; см. [ADR-005](adr/ADR-005-idempotency-ledger.md)). usage хранится в meta для аудита, на amount не влияет. | Нет (решено) |
| Q-005-1 | Конкретный JWT issuer / JWKS endpoint и flow выпуска токенов (Apple Sign-In? собственный auth? Firebase?). | **Closed (2026-06-02, решение пользователя)** | **Встроенный issuer в backend** ([ADR-018](adr/ADR-018-embedded-auth-issuer.md), модуль [auth](modules/auth/README.md)): backend САМ выпускает (`/v1/auth/*`) и верифицирует RS256 JWT (self-consistent, `iss=https://broadnova.shop`, `aud=claude-ios`). Первичная аутентификация — device-based (анонимная). Email/пароль и Apple Sign-In — опциональное расширение (Q-018-2 ниже). Prod-требование: сконфигурировать приватный ключ подписи (must-configure-before-launch). | Нет (решено) |
| Q-006-1 | Начисление кредитов: при покупке подписки (grant) и/или докупка пакетов? Endpoint начисления (`credit`) в ТЗ не описан. | **Closed** ([ADR-006](adr/ADR-006-credit-billing-and-subscription-grant.md)) | **Фиксированный пакет на период**: активация/продление подписки (subscription/sync, переход в active на новый период) начисляет `SUBSCRIPTION_CREDITS_PER_PERIOD` (config, дефолт 1000) кредитов как `credit`-tx, идемпотентно по `transactionId` периода. Публичный endpoint докупки пакетов — out of scope. | Нет (решено) |
| Q-007-1 | Sandbox vs production StoreKit verification, обработка отзыва/возврата (refund/revocation). | **Open — отложен (must-configure-before-launch)** | **На MVP — `STOREKIT_TEST_MODE`** (sandbox/test-режим, Apple prod certs не готовы, [TD-007](100-known-tech-debt.md)). Prod-верификация (`STOREKIT_TEST_MODE=false` + Apple root CA + реальный `APPSTORE_BUNDLE_ID` + заведённые IAP-продукты) — **обязательна до публичного запуска** ([07-deployment.md prod-checklist](07-deployment.md#prod-readiness-checklist-must-configure-before-launch)). refund → `subscription.status=expired`. | Нет (не блокирует инфру/staging; **prod-блокер до приёма пользователей**) |
| Q-009-1 | Атрибуция конкретного оператора в admin-API (нужен ли «кто именно из админов»)? | Open | На старте actor обезличен (`admin`), статический `ADMIN_API_SECRET` ([ADR-009](adr/ADR-009-admin-token-auth.md)). При появлении нескольких операторов с требованием атрибуции — переход на отдельный admin-JWT issuer (вариант B ADR-009) отдельным ADR. | Нет |
| Q-009-2 | Поведение admin-grant на несуществующем `userId`: `404` vs admin-provisioning. | Open | Дефолт **`404 user_not_found`** — admin-grant не создаёт пользователей (источник идентичности — JWT issuer, [ADR-007](adr/ADR-007-lazy-user-provisioning.md)); опечатка видна оператору. См. [modules/admin/02-api-contracts.md](modules/admin/02-api-contracts.md#обоснование-404-на-несуществующем-userid-не-admin-provisioning). | Нет |
| Q-010-1 | Точное значение TTL preview signed URL. | Open | Дефолт `PREVIEW_URL_TTL_SECONDS=900` (15 мин), конфигурируемо ([ADR-010](adr/ADR-010-backend-hosted-preview.md)). | Нет |
| Q-010-2 | Точные лимиты размера/числа файлов и content-type allowlist превью. | Open | Дефолты: файл ≤ 1 MB, проект ≤ 10 MB, ≤ 200 файлов; allowlist — html/css/js/json/png/jpeg/svg/gif/webp/woff2/plain. Конфигурируемо ([ADR-010](adr/ADR-010-backend-hosted-preview.md), [modules/website-builder/05-security.md](modules/website-builder/05-security.md)). | Нет |
| Q-010-3 | Изоляция origin превью: выделенный путь + sandbox-заголовки vs отдельный поддомен `preview.<domain>`. | Open | Старт — путь `/v1/preview/*` + sandbox CSP (самодостаточно). Prod-рекомендация (операционная) — отдельный поддомен. | Нет |
| Q-010-4 | Тарифицировать ли хранение/превью отдельно от генерации? | Open | Дефолт **нет**: генерация — обычный chat-шаг (1 кредит, [ADR-006](adr/ADR-006-credit-billing-and-subscription-grant.md)); хранение/превью бесплатны на старте. Пересмотр при росте объёма. | Нет |
| Q-017-1 | Конкретный домен сервиса для Traefik-роутинга (`Host(...)` rule + ACME-сертификат) ([ADR-017](adr/ADR-017-shared-server-traefik-deploy.md)). | **Closed (2026-06-02, значение пользователя)** | **`SERVICE_DOMAIN=broadnova.shop`** (HTTPS `https://broadnova.shop`). Предзапусковый операционный пункт: A-запись `broadnova.shop` → `87.239.135.154` (нужна для ACME-challenge внешнего Traefik) — см. [07-deployment.md prod-checklist](07-deployment.md#prod-readiness-checklist-must-configure-before-launch). | Нет (решено) |
| Q-017-2 | Имя ACME-certresolver общего Traefik (`/opt/edge`) для label `tls.certresolver` ([ADR-017](adr/ADR-017-shared-server-traefik-deploy.md)). | **Closed (2026-06-02, значение владельца сервера)** | **`TRAEFIK_CERTRESOLVER=le`** (`--certificatesresolvers.le.acme...`). Нюанс: `le` сделан **дефолтным** на entrypoint `websecure` (`--entrypoints.websecure.http.tls.certresolver=le`), поэтому label `tls.certresolver` в нашем `docker-compose` **опционален** (сертификат выпустится автоматически для любого HTTPS-роутера). Явный label `traefik.http.routers.<service>.tls.certresolver=le` **рекомендован для надёжности**. | Нет (решено) |

## Открытые вопросы встроенного auth-issuer (2026-06-02, [ADR-018](adr/ADR-018-embedded-auth-issuer.md))

| ID | Вопрос | Статус | Принятый дефолт (если есть) | Блокирует backend? |
|---|---|---|---|---|
| Q-018-1 | Усиление анти-Sybil для анонимной device-регистрации: достаточно ли per-IP rate-limit, нужен ли App Attest / DeviceCheck? | Open | Дефолт **per-IP rate-limit** (`AUTH_RATE_LIMIT_PER_IP=10/min`). App Attest/DeviceCheck — post-MVP усиление, путь не закрыт. Реализуемо на дефолте. | Нет |
| Q-018-2 | Апгрейд идентичности: добавлять ли email/пароль и/или Apple Sign-In (перенос идентичности между устройствами, account recovery)? | **Closed (2026-06-19, решение пользователя = Sign in with Apple)** | **Sign in with Apple** реализуется ([ADR-043](adr/ADR-043-sign-in-with-apple.md)): `POST /v1/auth/apple` — iOS шлёт Apple identity token (OIDC RS256), backend верифицирует и выдаёт НАШУ пару токенов; кросс-девайс аккаунт через `auth_identities` (UNIQUE `provider,subject`, миграция `0012`). Связывание: apple_sub известен → тот же `userId`; неизвестен → привязать к device-аккаунту без Apple-идентичности (сохранить кредиты/историю), иначе новый пользователь. **Email/пароль** остаётся опциональным расширением на будущее (не в этой поставке). Детали nonce/email/Services ID → [Q-043-1](#открытые-вопросы-sign-in-with-apple-2026-06-19-adr-043); авто-merge данных → [Q-043-2](#открытые-вопросы-sign-in-with-apple-2026-06-19-adr-043). | Нет (решено) |

## Открытые вопросы расширения Figma-gap (2026-06-02)

| ID | Вопрос | Статус | Принятый дефолт (если есть) | Блокирует backend? |
|---|---|---|---|---|
| Q-012-1 | Состав tool-реестра по `assistant_mode`: какие tools доступны Claude в `chat` vs `code` ([ADR-012](adr/ADR-012-assistant-mode-vs-billing-mode.md))? | Open | Дефолт: `code` — полный реестр (`files.*`, `site.*`, `calendar.*`, `reminders.*`); `chat` — без `site.*`/`files.*` (только `calendar.*`/`reminders.*`). Конфигурируемо. Реализуемо на дефолте. **Cross-ref [ADR-022](adr/ADR-022-optional-project-and-tool-gating.md):** `site.*` дополнительно гейтится наличием `project_id` поверх этого фильтра — итог `(assistant_mode допускает site.*) AND (project_id IS NOT NULL)` (И-композиция; фильтр по `assistant_mode` — не единственный). | Нет |
| Q-013-1 | Стратегия инъекции `workspace_files`/больших контекстов в prompt при росте объёма (полный текст vs усечение vs RAG) ([ADR-013](adr/ADR-013-workspace-projects-vs-website-builder.md)). | Open | Старт: простая вставка `extracted_text` с лимитом на суммарный размер контекста; усечение сверх лимита. RAG — будущее. | Нет |
| Q-014-1 | Полный allowlist media_type вложений (нужны ли DOCX/CSV/HEIC и пр.) ([ADR-014](adr/ADR-014-multimodal-attachments.md)). | **Заменён [Q-020-1](#q-020-1--allowlist-mediatype-вложений-mvp)** (ADR-014 superseded транспортом [ADR-020](adr/ADR-020-inline-base64-attachments-mvp.md)). | — | Нет |
| Q-014-2 | Точные лимиты вложений (размер/число на сообщение) ([ADR-014](adr/ADR-014-multimodal-attachments.md)). | **Заменён [Q-020-2](#q-020-2--лимиты-вложений-mvp)** (ADR-014 superseded транспортом [ADR-020](adr/ADR-020-inline-base64-attachments-mvp.md)). | — | Нет |
| **Q-015-1** | **Покупка токенов (consumable IAP) без активной подписки — продуктовое противоречие монетизации MVP.** | **Closed (2026-06-02, решение пользователя = вариант B)** | **Покупка токенов требует активной подписки** (докупка сверх месячного пакета). Без активной подписки `POST /v1/tokens/purchase` → `403 subscription_required` (policy-guard **до** grant). Сохраняет §2 ТЗ и [ADR-002](adr/ADR-002-access-policy-state-machine.md) без изменений; устраняет «мёртвый» баланс. См. [ADR-015 §Доступность](adr/ADR-015-consumable-token-iap.md). | Нет (решено) |
| **Q-016-1** | **Actions** (Plan Week, Meeting Notes, Tasks from Photo, Summarize Text, Project Structure, Daily Review, Design Brief) и **Choose style** (Normal/Learning/Concise/Formal): серверные пресеты (endpoint + хранение) ИЛИ клиентские (iOS подставляет промпт)? | **Open — для пользователя** | Дефолт **клиентские пресеты**: iOS формирует готовый промпт/`assistantMode`/`context`, backend получает уже готовое сообщение — **новый backend-эндпоинт не нужен**. Стиль ответа можно передавать через `context.style` (информативно). Реализуемо без backend-работы. | Нет |
| **Q-016-2** | **Web search** (toggle в Add To Chat): backend-tool (серверная интеграция веб-поиска, как `site.*`) ИЛИ клиентская фича? | **Open — для пользователя, НЕ реализуем до ответа** | Дефолт-намерение: backend server-side tool, но требует **выбора провайдера поиска** (Brave/Bing/Anthropic web_search и т.п.) и тарификации → **не реализуем до ответа пользователя**. Без выбора провайдера — нет контракта tool. | **Да — блокер для фичи web search** (не для остального) |

## Открытые вопросы мультимодальных вложений (MVP, 2026-06-03, [ADR-020](adr/ADR-020-inline-base64-attachments-mvp.md))

| ID | Вопрос | Статус | Принятый дефолт (если есть) | Блокирует backend? |
|---|---|---|---|---|
| Q-020-1 | Полный allowlist `mediaType` вложений: нужны ли DOCX/XLSX/HEIC/SVG и пр.? | Open | Старт: `image/jpeg|png|gif|webp`, `application/pdf`, `text/plain|markdown|csv`, `application/json`. Расширение по мере необходимости. Реализуемо на дефолте. | Нет |
| Q-020-2 | Точные значения `ATTACHMENT_*` лимитов (размер одного/суммарный, число, PDF-страницы, повышенный body-лимит `/chat/run`). | Open | Дефолты: `ATTACHMENT_MAX_COUNT=10`, `ATTACHMENT_MAX_BYTES_IMAGE=5 MB` / `ATTACHMENT_MAX_BYTES_DOCUMENT=8 MB`, `ATTACHMENT_TOTAL_BYTES=10 MB`, `ATTACHMENT_PDF_MAX_PAGES=100`, `ATTACHMENT_REQUEST_BODY_LIMIT=12 MB`. Конфигурируемо ([TD-004](100-known-tech-debt.md)-аналог — не калибровано на нагрузке). Согласованы с лимитами Anthropic по vision/PDF. | Нет |

### Q-020-1 — allowlist mediaType вложений (MVP)
Заменяет Q-014-1 (транспорт ADR-014 superseded). Дефолтный allowlist покрывает сценарии дизайна (фото, PDF, текстовые/csv/json-файлы). DOCX/XLSX требуют конвертации/извлечения — вне MVP. HEIC (iOS-фото) — кандидат на расширение, но iOS может конвертировать в JPEG на клиенте; решение отложено.

### Q-020-2 — лимиты вложений (MVP)
Заменяет Q-014-2. Дефолты эвристические, согласованы с ограничениями Anthropic Messages API (vision-изображения и PDF-document по размеру/разрешению). Калибровка по прод-метрикам — как [TD-004](100-known-tech-debt.md).

## Открытый вопрос консистентности run↔история ([ADR-024](adr/ADR-024-history-payload-domain-normalization.md), 2026-06-10)

| ID | Вопрос | Статус | Принятый дефолт (если есть) | Блокирует backend? |
|---|---|---|---|---|
| Q-024-1 | При `status=tool_call` `ChatResponse` сейчас **отбрасывает** сопутствующий текст, который Claude выдал вместе с `tool_use` (`orchestrator.py:661`). Должен ли `/chat/run` (и `/chat/tool-result`) ТАКЖЕ возвращать этот текст как опц. `assistantMessage` при `tool_call` — для полной консистентности с историей (где шаг хранит `[text, tool_use]` вместе)? | **Closed (2026-06-10, решение пользователя = вариант A)** | **Вариант A:** при `status=tool_call` ответ `/chat/run` и `/chat/tool-result` опционально возвращает `assistantMessage` — текст из `text`-блоков того же assistant-шага (того, чей `tool_use` вернулся как `toolCall`). При отсутствии текста — `null`/опущено. Аддитивно/обратносовместимо (поле уже nullable; меняется лишь то, что теперь может быть НЕ-null при `tool_call`). Backend перестаёт отбрасывать текст (`orchestrator.py:661`). Семантика `assistantMessage` при `assistant_message` (финал) и `blocked` (`null`) — без изменений. См. [ADR-024 §Decision п.3](adr/ADR-024-history-payload-domain-normalization.md), [chat-orchestrator/02-api-contracts.md](modules/chat-orchestrator/02-api-contracts.md#response-200). | **Нет для ADR-024** (нормализация истории — нестыковки 1/2 + полнота на стороне истории — закрыта независимо). Влияет только на enrichment `ChatResponse`. |

### Q-024-1 — сопутствующий текст при `tool_call` (run↔история)
Нестыковка 3 из [ADR-024](adr/ADR-024-history-payload-domain-normalization.md): assistant-ход может нести в одном сообщении и `text`, и `tool_use`; история отдаёт оба блока шага (после доменной нормализации), а `ChatResponse` при `status=tool_call` возвращает только `toolCall`, теряя текст. ADR-024 **на стороне истории** уже даёт полный шаг (клиент берёт текст из `steps[].payload` по `stepId`). Остаётся продуктовый вопрос — дотягивать ли сам `ChatResponse`:
- **Вариант A (рекомендован):** добавить опц. `assistantMessage` в ответ при `status=tool_call`; backend перестаёт отбрасывать текст (`orchestrator.py:661`), кладёт его в `assistantMessage`. Плюс: прогресс-UI сразу показывает «Claude сказал X и вызвал инструмент Y» без запроса истории. Аддитивно (поле уже опционально).
- **Вариант B:** не менять `ChatResponse`; задокументировать, что полный ход доступен в истории по `stepId`/`messageStepId` ([ADR-023](adr/ADR-023-sync-ids-in-chat-response.md)). Минус: доп. round-trip/задержка для показа сопутствующего текста.

**Resolution (2026-06-10, решение пользователя = вариант A).** При `status=tool_call` `ChatResponse` (`/chat/run` и `/chat/tool-result`) **опционально** возвращает `assistantMessage` — текст из `text`-блоков **того же** assistant-шага, чей `tool_use` вернулся как `toolCall` (тот же шаг, на который указывает `stepId`, [ADR-023](adr/ADR-023-sync-ids-in-chat-response.md)). Значение = конкатенация/текст этих `text`-блоков; при их отсутствии `assistantMessage = null`/опущено. Backend перестаёт отбрасывать текст (`orchestrator.py:661`) и кладёт его в `assistantMessage`. Согласовано с нормализацией истории: текст в `GET /v1/chats/{id}` → `steps[].payload.content[]` того же шага (по `stepId`) — **тот же**. Изменение аддитивно/обратносовместимо: поле уже было опционально-nullable; меняется лишь то, что оно теперь может быть НЕ-null при `tool_call`; остальные поля `tool_call` (`toolCall` обязателен, `toolCall.id`/`name` доменные) не меняются. `assistantMessage` при `status=assistant_message` (финал) — без изменений; при `status=blocked` — `null`. Cross-ref: [ADR-024 §Decision п.3](adr/ADR-024-history-payload-domain-normalization.md), [chat-orchestrator/02-api-contracts.md §Response (200)](modules/chat-orchestrator/02-api-contracts.md#response-200), [chat-orchestrator/09-testing.md §ADR-024](modules/chat-orchestrator/09-testing.md), [API-REFERENCE.md §5](API-REFERENCE.md).

## Открытые вопросы унификации контракта календаря ([ADR-027](adr/ADR-027-calendar-read-contract-alignment.md), 2026-06-11)

| ID | Вопрос | Статус | Принятый дефолт (если есть) | Блокирует backend? |
|---|---|---|---|---|
| Q-027-1 | Перевести ли календарные `start`/`end` (`calendar.read` + `calendar.create_events`) на **tz-aware** ISO8601 (с offset / UTC) вместо naive local? | **Open — отложен** | **На MVP — naive local datetime без offset** (`"2026-06-11T09:00:00"`), как сложившийся де-факто формат `create_events`; `calendar.read` выровнен под него ([ADR-027 §Decision 2](adr/ADR-027-calendar-read-contract-alignment.md)). Tz-aware — будущее усиление **обоих** инструментов симметрично, отдельным ADR; iOS оперирует локальным временем устройства, на MVP достаточно. | Нет (MVP работает на naive local) |
| Q-027-2 | Нужна ли миграция уже сохранённых сессий (исторические `tool_call` в `chat_steps`/`tool_calls` с прежними `startDate`/`endDate` date-only)? | **Closed (2026-06-11)** | **Миграция не требуется.** Исторические шаги хранят то, что было сгенерировано на момент записи; реплей/история (`GET /v1/chats/{id}`) отдаёт `tool_use.input` как есть — нормализация ([ADR-024](adr/ADR-024-history-payload-domain-normalization.md)) не переписывает имена/значения аргументов внутри payload. Влияние ограничено отображением старых шагов; новые вызовы используют `start`/`end` datetime. Backfill бессмыслен (date-only-значения корректны для своих исторических вызовов). | Нет (решено) |

### Q-027-1 — tz-aware vs naive local для календаря
Цель [ADR-027](adr/ADR-027-calendar-read-contract-alignment.md) — выровнять `calendar.read` под **существующий** формат `calendar.create_events` (naive local datetime), не реформируя create. Переход на tz-aware затронул бы оба инструмента и контракт create без необходимости на MVP. Если появятся кросс-таймзонные сценарии (события в чужой зоне, серверная нормализация времени) — вводить tz-aware **симметрично** для read и create отдельным ADR с явной конвенцией offset.

### Q-027-2 — миграция исторических календарных вызовов (Closed)
Решено: миграции нет. Старые `calendar.read`-вызовы с date-only `startDate`/`endDate` остаются в истории как есть и валидны для своих исторических ходов; нормализация истории ([ADR-024](adr/ADR-024-history-payload-domain-normalization.md)) их не трогает (переписывает только доменные имена инструментов и id-блоков, не содержимое `tool_use.input`). Замена контракта вступает в силу для **новых** вызовов после релиза iOS.

## Q-015-1 — продуктовое противоречие монетизации MVP (Closed)

**Статус:** Closed — 2026-06-02, решение пользователя. **Выбран вариант (б): покупка токенов требует активной подписки** (докупка сверх месячного пакета).

**Выявлено qa+reviewer (2026-06-02)** при синхронизации реализованного token-purchase с policy: при прежнем дефолте «покупка разрешена всем» пользователь без подписки мог купить кредиты, но не мог их потратить (потребление в `credits`-mode гейтится [ADR-002](adr/ADR-002-access-policy-state-machine.md): без подписки доступен только trial) — «мёртвый» баланс.

**Принятое решение (вариант б).** Покупка токенов осмысленна только при активной подписке: `POST /v1/tokens/purchase` гейтится policy-guard'ом — `subscription.status == active` проверяется **до** `WalletService.grant`. Без активной подписки → HTTP `403` с `code=subscription_required`, кредиты **не** начисляются, ledger не пишется. Покупка = докупка кредитов **сверх** `SUBSCRIPTION_CREDITS_PER_PERIOD` для подписчиков. Это **сохраняет** §2 ТЗ и [ADR-002](adr/ADR-002-access-policy-state-machine.md) без изменений (policy-engine и правило потребления не меняются), устраняет «мёртвый» баланс. Вариант (а) (policy-патч, разблокировка `credits`-mode без подписки покупкой) — **отвергнут**.

**Cross-ref:** [ADR-015 §Доступность](adr/ADR-015-consumable-token-iap.md), [modules/token-purchase/README.md](modules/token-purchase/README.md), [modules/token-purchase/02-api-contracts.md](modules/token-purchase/02-api-contracts.md), [figma-gap-analysis.md](figma-gap-analysis.md#mvp-scope-решение-пользователя-2026-06-02), [API-REFERENCE.md §20](API-REFERENCE.md#20-tokens).

## Q-017-1 / Q-017-2 — deploy-значения домена и Traefik certresolver (Closed)

**Статус:** Closed — 2026-06-02, финальные deploy-значения от пользователя/владельца сервера ([ADR-017](adr/ADR-017-shared-server-traefik-deploy.md)).

**Q-017-1 (домен).** `SERVICE_DOMAIN=broadnova.shop`, публичный URL `https://broadnova.shop`. Предзапусковое операционное требование (не код): A-запись `broadnova.shop` → `87.239.135.154` должна существовать **до** запуска — она нужна внешнему Traefik для ACME HTTP-challenge при выпуске Let's Encrypt-сертификата. Остаётся пунктом [prod-checklist](07-deployment.md#prod-readiness-checklist-must-configure-before-launch).

**Q-017-2 (certresolver).** `TRAEFIK_CERTRESOLVER=le` (`--certificatesresolvers.le.acme...` в конфиге общего Traefik `/opt/edge`). Владелец сервера сделал `le` **дефолтным** резолвером на entrypoint `websecure` (`--entrypoints.websecure.http.tls.certresolver=le`), поэтому сертификат выпускается автоматически для любого HTTPS-роутера, и label `traefik.http.routers.<service>.tls.certresolver=le` в нашем `docker-compose` **опционален**. Тем не менее явный label **рекомендован для надёжности** (детерминированность при возможном изменении дефолта на entrypoint).

**Cross-ref:** [ADR-017](adr/ADR-017-shared-server-traefik-deploy.md), [07-deployment.md](07-deployment.md#маршрутизация-через-traefik-docker-labels), [07-deployment.md prod-checklist](07-deployment.md#prod-readiness-checklist-must-configure-before-launch).

## Q-017-3 — выкат второго инстанса avelyraweb.shop (мульти-инстанс) — Closed

> **Историческая запись (fork-ancestry, неактуальна для `claude-hermes`).** Относится к мульти-инстанс-развёртыванию **claude-ios** на общем сервере `.154`. `claude-hermes` — самостоятельный сервис на выделенном `.156` (`avorelio.shop`, [ADR-057](adr/ADR-057-dedicated-server-self-hosted-traefik-deploy.md)); `INSTANCES`-loop **удалён** из его CI ([ADR-017 §Ревизия 2026-07-15](adr/ADR-017-shared-server-traefik-deploy.md#ревизия-2026-07-15--для-claude-hermes-применяется-только-выделенный-156-adr-057)). `avelyra`/`avelyraweb.shop` — отдельный сервис, из этого репозитория не деплоится.

**Статус:** **Closed / Resolved — 2026-06-10.** Второй инстанс `avelyra` (`avelyraweb.shop`) развёрнут на сервере и включён в `INSTANCES` обоих deploy-workflow. Живой broadnova.shop не затронут.

**Итог выката (2026-06-10).** Все next-step операции devops выполнены и проверены ревью:
1. `docker-compose.prod.yml` параметризован через `${COMPOSE_PROJECT_NAME:-claude-ios}` (image + Traefik router/service-имена); инвариант обратной совместимости проверен (`compose config` для существующего `/opt/claude-ios/.env` идентичен — broadnova не затронут).
2. Закомментированный `COMPOSE_PROJECT_NAME` (дефолт `claude-ios`) добавлен в `.env.prod.example`.
3. `INSTANCES`-loop внедрён в **оба** workflow (`ci.yml` gated deploy-job + ручной `deploy.yml`): `INSTANCES="claude-ios:claude-ios avelyra:avelyra"`, **claude-ios первым** (backward-compat no-op).
4. `/opt/avelyra` провизионирован (git clone того же репо, свой `.env`, **свежий** JWT keypair в `/opt/avelyra/.secrets/`, **свежие** секреты `POSTGRES_PASSWORD`/`ANTHROPIC_API_KEY`/`ADMIN_API_SECRET`/`KMS_LOCAL_MASTER_KEY`/`PREVIEW_URL_SECRET`/`METRICS_SCRAPE_TOKEN`) и выкачен `-p avelyra`. `GET https://avelyraweb.shop/healthz` и `/docs` → `200`; round-trip auth зелёный; `broadnova.shop/healthz` → `200` (сосед не затронут).

**Изоляция (подтверждена).** Инстансы полностью изолированы по данным, секретам и JWT keypair: раздельные тома `avelyra_pgdata`/`claude-ios_pgdata`, раздельные `.secrets/`, у avelyra **собственные** секреты (не копии broadnova). Разделяются только внешняя сеть `web` + общий edge-Traefik (`/opt/edge`).

**Зафиксированные параметры клона avelyra:**
- `COMPOSE_PROJECT_NAME=avelyra`, `SERVICE_DOMAIN=avelyraweb.shop`, `JWT_ISSUER=https://avelyraweb.shop`, `JWT_AUDIENCE=claude-ios` (по умолчанию переиспользован — отдельного iOS-приложения/bundle у клона пока нет), `TRAEFIK_CERTRESOLVER=le` (общий резолвер).
- **`ANTHROPIC_API_KEY` переиспользован по согласованию** (отступление от рекомендации «свой ключ на инстанс» — принято осознанно на старте; при росте нагрузки/раздельном биллинге развести по отдельным ключам).
- **Staging-паритет с broadnova:** `DOCS_ENABLED=true` (Swagger доступен), `STOREKIT_TEST_MODE=true` (Apple prod-certs отложены, [Q-007-1](99-open-questions.md)/[TD-007](100-known-tech-debt.md)).

**Остаточные prod-блокеры avelyra (must-configure-before-launch, применяются к КАЖДОМУ инстансу отдельно — см. [prod-checklist](07-deployment.md#prod-readiness-checklist-must-configure-before-launch)):** перед публичным запуском avelyra выключить staging-режимы — `DOCS_ENABLED=false`, `STOREKIT_TEST_MODE=false` + Apple prod-certs/реальный `APPSTORE_BUNDLE_ID`/заведённые IAP — и сконфигурировать собственный JWT signing key (свежая RSA-пара уже в `/opt/avelyra/.secrets/`). Это не блокирует staging — avelyra работает в staging-паритете с broadnova.

**Cross-ref:** [ADR-017 §Мульти-инстанс](adr/ADR-017-shared-server-traefik-deploy.md), [07-deployment.md §Мульти-инстанс](07-deployment.md#мульти-инстанс--клонирование-сервиса), [07-deployment.md §CI/CD INSTANCES-loop](07-deployment.md#cicd-контракт-instances-loop-мульти-инстанс), [ADR-018](adr/ADR-018-embedded-auth-issuer.md) (per-instance JWT keypair).

## Открытые вопросы инструмента `time.now` (2026-06-10, [ADR-026](adr/ADR-026-global-server-side-tools-and-time-now.md))

| ID | Вопрос | Статус | Принятый дефолт (если есть) | Блокирует backend? |
|---|---|---|---|---|
| Q-026-1 | Лимит длины аргумента `tz` инструмента `time.now` и дефолтная таймзона сервиса. | **Closed (2026-06-10, дефолты)** | **Лимит длины `tz` ≤ 64 символа** (длиннее любого валидного IANA-имени; вне лимита → tool-result error `invalid_timezone`, не падение хода). **Дефолтная tz — UTC** (при отсутствии `tz` результат только в UTC; локального представления нет). Реализовано: `TIME_NOW_TZ_MAX_LENGTH = 64` (проверка в handler `GlobalToolHandlers`, не pydantic `max_length` — чтобы over-limit деградировал к tool-result error, а не 422 хода); дефолт UTC. | Нет |

### Q-026-1 — лимит длины `tz` и дефолтная таймзона (Closed 2026-06-10)
Аргумент `tz` инструмента `time.now` ([ADR-026 §6](adr/ADR-026-global-server-side-tools-and-time-now.md)) — опциональный IANA-имя зоны. **Закрыт принятием дефолтов (реализованы):** длина `≤ 64` (`TIME_NOW_TZ_MAX_LENGTH = 64`, проверка в handler `GlobalToolHandlers`, не pydantic — over-limit → tool-result error, не 422 хода; защита от мусорного/раздутого ввода до резолва `zoneinfo`); дефолтная зона сервиса — **UTC** (без `tz` модель получает UTC-набор `utc`/`unix`/`weekday`; локальное `local`/`timezone` — только при явном валидном `tz`). Невалидный/длинный `tz` → tool-result error `invalid_timezone`, ход продолжается. [TD-019](100-known-tech-debt.md) (tz-база в prod-образе) — **Resolved** (зависимость `tzdata` добавлена), `tz` в prod работает; UTC работает всегда независимо.

## Открытые вопросы аддитивных правок контракта iOS (2026-06-12, [ADR-028](adr/ADR-028-projectid-in-chat-list-and-server-tools-in-chat-response.md))

| ID | Вопрос | Статус | Принятый дефолт (если есть) | Блокирует backend? |
|---|---|---|---|---|
| Q-028-1 | Формат `summary` элемента `serverTools[]` — единый компактный (`"ok"` для всех `completed`) ИЛИ доменный per-tool (например имя файла для `site.write_file`, weekday для `time.now`)? | **Closed (2026-06-12, дефолт = единый компактный формат, реализован)** | **Единый компактный формат (финальное MVP-решение):** `completed` → литерал `"ok"`, `errored` → короткий `error_code` (например `invalid_timezone`); обрезка до `_SUMMARY_MAX_CHARS=120`. **Без raw result, без `error_message`, без путей/URL/signed-token** (реализовано в `_server_tool_summary`, безопасность/размер — [ADR-028 §Решение 2](adr/ADR-028-projectid-in-chat-list-and-server-tools-in-chat-response.md)). Per-tool обогащение — отдельная задача по запросу, не MVP; контракт поля не меняется. | Нет (решено) |

### Q-028-1 — формат `summary` для server-side инструментов (Closed 2026-06-12)
**Статус:** Closed — реализация зафиксировала единый компактный формат, это и есть финальное MVP-решение. `serverTools[].summary` ([ADR-028](adr/ADR-028-projectid-in-chat-list-and-server-tools-in-chat-response.md)) — компактный индикатор итога, **не** канал доставки результата (полный результат — в истории `GET /v1/chats/{id}` после нормализации [ADR-024](adr/ADR-024-history-payload-domain-normalization.md)).

**Принятое решение (дефолт, реализован в `_server_tool_summary`):** `summary` несёт **строго** один из двух вариантов — `completed` → литерал `"ok"`; `errored` → короткий машинный `error_code` (например `invalid_timezone`). **Никогда** не читаются и не попадают в ответ: raw result, `error_message`/детали ошибки, стектрейсы, пути, URL, signed-token (`site.*` может вернуть пути/URL/signed-token — они исключены by-design). Обрезка до `_SUMMARY_MAX_CHARS=120`.

**Per-tool обогащение — отдельная будущая задача (не MVP).** Если iOS-UX потребует более информативного доменного per-tool-итога (без утечки секретов) — вводится отдельным уточнением/feature по реальной потребности; контракт поля (тип `string|null`, лимит 120, `status` ∈ {completed,errored}) при этом **не** меняется. Закрытие Q-028-1 дефолтом путь к обогащению не закрывает — это самостоятельная аддитивная правка при запросе.

## Открытые вопросы Adapty subscription webhook (2026-06-12, [ADR-029](adr/ADR-029-adapty-subscription-webhook.md))

| ID | Вопрос | Статус | Принятый дефолт (если есть) | Блокирует backend? |
|---|---|---|---|---|
| Q-029-1 | Переносить ли consumable-пакеты токенов на Adapty (сейчас `/v1/tokens/purchase`, StoreKit IAP, [ADR-015](adr/ADR-015-consumable-token-iap.md))? | **Open (отложено, не MVP-этой-итерации)** | На этой итерации **НЕТ**: consumable остаётся на `/v1/tokens/purchase`. Adapty-вебхук покрывает только 4 события подписки. Перенос — отдельная итерация (новые события Adapty consumable + idempotency). См. [TD-020](100-known-tech-debt.md). | Нет (вне scope) |
| Q-029-2 | Ретировать ли `/v1/subscription/sync` (StoreKit JWS) после стабилизации Adapty-пути? | **Closed (2026-06-24, ретировать — для изолированного claude-hermes)** | **ДА — ретировать** (ревизия [ADR-029](adr/ADR-029-adapty-subscription-webhook.md)): для нового изолированного сервиса claude-hermes StoreKit-путь подписок `POST /v1/subscription/sync` удаляется (роут + подписочная ветка кода), Adapty-вебхук остаётся единственным путём подписок. Двойное начисление устраняется by construction (второго пути нет), а не контрактной митигацией. `/v1/tokens/purchase` (consumable, [ADR-015](adr/ADR-015-consumable-token-iap.md)) сохраняется. Закрывает [TD-021](100-known-tech-debt.md). | Нет |
| Q-029-3 | Точные строковые значения `event_type` Adapty (и набор полей payload) в продакшн-конфиге Adapty проекта iOS-команды. | **Open (требует сверки с Adapty-конфигом)** | Дефолт: распознаём `subscription_started`/`subscription_renewed`/`subscription_cancelled`/`subscription_expired` (lower). Парсинг **устойчив**: неизвестный тип/поле → `200 ignored` (не сбой), поэтому **не блокирует** реализацию. До сверки оператор сопоставляет реальные имена событий Adapty с этими четырьмя (через настройку маппинга в Adapty UI или подтверждение совпадения имён). | Нет (парсинг fail-safe) |

### Q-029-1 / Q-029-2 / Q-029-3 — детали
- **Q-029-1 (consumables через Adapty).** Текущая итерация сознательно ограничена подписками. Consumable-логика (`token_products()`, idempotency по consumable `transactionId`, policy-guard «требует подписки» [Q-015-1](99-open-questions.md)) остаётся на StoreKit-пути. При переносе — спроектировать consumable-события Adapty и их idempotency отдельно ([TD-020](100-known-tech-debt.md)).
- **Q-029-2 (ретирование sync) — Closed (2026-06-24, ретировать).** Решено: для изолированного claude-hermes `POST /v1/subscription/sync` **ретируется** (удаление роута + подписочной ветки кода; ревизия [ADR-029](adr/ADR-029-adapty-subscription-webhook.md), см. INDEX §Ревизии). Adapty-вебхук — **единственный** путь подписок → двойное начисление устранено **by construction** (второго пути нет), контрактная митигация «клиент использует один путь» больше не нужна. `/v1/tokens/purchase` (consumable, [ADR-015](adr/ADR-015-consumable-token-iap.md)) сохраняется (отдельный продукт). Реализация — backend ([TD-021](100-known-tech-debt.md)).
- **Q-029-3 (имена событий).** Backend реализуется на дефолтных четырёх именах; устойчивость парсинга (неизвестное → `ignored`) снимает блокировку. Оператор обязан сверить реальные `event_type` Adapty до приёма прод-трафика (иначе валидные события молча уйдут в `ignored` — корректно по контракту, но без эффекта). Пункт сверки — в prod-checklist Adapty (см. [07-deployment.md](07-deployment.md)).

## Открытые вопросы провайдер-абстракции LLM (2026-06-16, [ADR-033](adr/ADR-033-llm-provider-abstraction.md))

| ID | Вопрос | Статус | Принятый дефолт (если есть) | Блокирует backend? |
|---|---|---|---|---|
| Q-033-1 | Хранить ли в `chat_steps.payload` нейтральный (провайдер-агностичный) формат вместо wire-формата активного провайдера — для кросс-провайдерного реплея в одной БД? | **Open (отложено, не требуется на MVP)** | **НЕТ** на MVP: инстанс одно-провайдерный, payload = wire-формат активного провайдера ([ADR-033 §3](adr/ADR-033-llm-provider-abstraction.md)), реплей всегда через тот же клиент. Кросс-провайдерный реплей не требуется. Введение нейтрального формата хранения — отдельный ADR при реальной потребности. См. [TD-024](100-known-tech-debt.md). | Нет (решено дефолтом) |
| Q-033-2 | Перейти ли на OpenAI **Responses API** вместо Chat Completions? | **Open (отложено)** | **НЕТ** на MVP: Chat Completions — зрелый function-calling + vision, прямой паритет с текущим non-streaming Anthropic-путём. Responses API — при потребности (stateful/tools-расширения). | Нет |
| Q-033-3 | Домен, каталог `/opt/<dir>` и `-p <project>` для 3-го (OpenAI) инстанса; реальный `OPENAI_API_KEY` с балансом. | **Open (предоставляются позже)** | Параметры даст владелец инфраструктуры/пользователь при развёртывании. Код провайдер-абстракции от них не зависит ([ADR-033](adr/ADR-033-llm-provider-abstraction.md)); это операционный пункт деплоя ([07-deployment.md §Мульти-инстанс](07-deployment.md#мульти-инстанс--клонирование-сервиса)). | Нет (операционный) |

### Q-033-1 / Q-033-2 / Q-033-3 — детали
- **Q-033-1 (формат персиста).** На MVP `chat_steps.payload` хранит wire-формат провайдера инстанса (Anthropic content-блоки ИЛИ OpenAI assistant-message). Это допустимо, т.к. инстанс одно-провайдерный и реплей идёт через тот же клиент. Если потребуется кросс-провайдерный реплей (миграция данных между провайдерами, A/B одного диалога) — спроектировать нейтральный формат хранения + двунаправленную трансляцию (отдельный ADR, [TD-024](100-known-tech-debt.md)).
- **Q-033-2 (Responses API).** Chat Completions покрывает function-calling (`tools`/`tool_calls`) и vision (`image_url`) — достаточно для паритета. Переход на Responses API — при необходимости его возможностей.
- **Q-033-3 (параметры 3-го инстанса).** Backend готов к `LLM_PROVIDER=openai` независимо от конкретных домена/ключа; провижининг — по существующей процедуре клонирования + `LLM_PROVIDER`/`OPENAI_*`.

## Открытые вопросы выбора модели (2026-06-17, [ADR-034](adr/ADR-034-user-model-selection.md))

| ID | Вопрос | Статус | Принятый дефолт (если есть) | Блокирует backend? |
|---|---|---|---|---|
| Q-034-1 | Дифференцированная тарификация по модели (премиум-модели дороже 1 кредита)? | **Open (отложено)** | **НЕТ** на этой поставке: 1 кредит = 1 сообщение независимо от модели ([ADR-006](adr/ADR-006-credit-billing-and-subscription-grant.md)). Тарифные коэффициенты по модели — будущая поставка плана model/presets. См. [TD-025](100-known-tech-debt.md). | Нет (решено дефолтом) |
| Q-034-2 | Presets (предустановленные конфиги «модель + assistantMode + параметры») как отдельная сущность/эндпоинт? | **Open (отложено)** | Поставка 1 покрывает только выбор модели. Presets — следующие поставки плана model/presets; формат (env / БД / эндпоинт) проектируется отдельным ADR при старте поставки. | Нет |
| Q-034-3 | Смена модели в существующей сессии (не session-fixed) / per-message override? | **Open (отложено)** | **НЕТ** на MVP: `model` session-fixed (как `mode`/`assistantMode`, [ADR-034 §3](adr/ADR-034-user-model-selection.md)) — консистентная история. Смена модели по ходу диалога — при потребности отдельным ADR (новая сессия — текущий путь смены модели). | Нет (решено дефолтом) |

### Q-034-1 / Q-034-2 / Q-034-3 — детали
- **Q-034-1 (тарификация).** Биллинг неизменен: выбор дорогой/дешёвой модели не меняет списание (1 кредит). Если потребуется ценовая дифференциация — ввести коэффициент стоимости per-model (env `MODEL_CREDIT_COST` или поле в allowlist-объекте) + правку debit в orchestrator; идемпотентность ([ADR-005](adr/ADR-005-idempotency-ledger.md)) сохранить. Отдельный ADR.
- **Q-034-2 (presets).** Поставка 1 — атомарный выбор модели. Presets как **текстовые пресеты промтов** (чипы экрана 4) реализованы в Поставке 2 ([ADR-035](adr/ADR-035-prompt-presets-endpoint.md), `GET /v1/presets`). Presets как **именованные комбинации модель+режим+системные настройки** — отдельная надстройка, остаётся открытой (могут лечь на тот же эндпоинт аддитивными полями или на новую сущность); проектируется при старте соответствующей поставки.
- **Q-034-3 (смена модели в сессии).** Текущий контракт: чтобы сменить модель — начать новую сессию (как для `mode`/`assistantMode`). Per-message/in-session смена — отдельная фича.

## Открытые вопросы пресетов промтов (2026-06-17, [ADR-035](adr/ADR-035-prompt-presets-endpoint.md))

| ID | Вопрос | Статус | Принятый дефолт (если есть) | Блокирует backend? |
|---|---|---|---|---|
| Q-035-1 | Редактирование пресетов оператором без деплоя (таблица БД + admin-CRUD)? | **Open (отложено)** | **НЕТ** на старте: статический реестр в коде ([ADR-035 §2](adr/ADR-035-prompt-presets-endpoint.md)). Промежуточный шаг без БД — config-JSON env ([TD-026](100-known-tech-debt.md)). БД-CRUD — пост-MVP при подтверждённой потребности; контракт `GET /v1/presets` не меняется (источник инкапсулирован). | Нет (решено дефолтом) |
| Q-035-2 | Локализация `title`/`prompt` пресетов (per-locale наборы / `Accept-Language` / клиентский перевод по `id`)? | **Open (отложено)** | **НЕТ** i18n на старте: единый язык EN ([ADR-035 §5](adr/ADR-035-prompt-presets-endpoint.md)). Переход аддитивен (`id` стабилен) — можно добавить локализованные поля/ветвление, не ломая клиента. | Нет (решено дефолтом) |

## Открытые вопросы Workspaces (2026-06-17, [ADR-036](adr/ADR-036-workspaces-implementation.md))

| ID | Вопрос | Статус | Принятый дефолт (если есть) | Блокирует backend? |
|---|---|---|---|---|
| Q-036-1 | Точные значения лимитов файлов-знаний (`WORKSPACE_FILE_MAX_COUNT`/`WORKSPACE_FILE_MAX_BYTES`/`WORKSPACE_FILES_TOTAL_BYTES`) и лимита инжектируемого контекста (`WORKSPACE_CONTEXT_MAX_CHARS`). | Open | Дефолты ([ADR-036 §4,§6](adr/ADR-036-workspaces-implementation.md)): `WORKSPACE_FILE_MAX_COUNT=20`, `WORKSPACE_FILE_MAX_BYTES=8 MB` (= document-cap вложений), `WORKSPACE_FILES_TOTAL_BYTES=32 MB`, `WORKSPACE_CONTEXT_MAX_CHARS=200000`. Конфигурируемы; эвристические, калибровка по прод-метрикам — как [TD-004](100-known-tech-debt.md). | Нет (реализуемо на дефолтах) |
| Q-036-2 | Стратегия усечения при превышении `WORKSPACE_CONTEXT_MAX_CHARS` (порядок файлов, частичное усечение, приоритизация). | **Перенаправлен в [Q-013-1](#)** | Старт ([ADR-036 §6](adr/ADR-036-workspaces-implementation.md)): порядок `created_at` ASC (старые первыми), усекается хвост `extracted_text`; RAG/семантический отбор — [Q-013-1](#). | Нет (дефолт-усечение) |
| Q-036-3 | Поведение при дубликате имени файла (`filename`) в одном workspace — `409` vs разрешить дубликаты? | **Closed (дефолт)** | **Разрешены дубликаты** (нет UNIQUE по `(workspace_project_id, filename)`): `id` (uuid) — единственный ключ файла; iOS может загрузить одноимённые версии. Разметка контекста `[Файл проекта: {filename}]` допускает повторы. | Нет (решено) |

> Q-036-* (детали): API-путь (`/v1/workspaces`), стратегия хранения (BYTEA, не attachments), инъекция, удаление (CASCADE/SET NULL), пагинация, биллинг — **закрыты решениями [ADR-036](adr/ADR-036-workspaces-implementation.md)**, не заводятся как открытые вопросы. Остаются только калибровка лимитов (Q-036-1) и усечение (Q-036-2 → [Q-013-1](#)).

## Открытые вопросы `context` (2026-06-17, [ADR-037](adr/ADR-037-chatrunrequest-context-allowlist-injection.md))

| ID | Вопрос | Статус | Принятый дефолт (если есть) | Блокирует backend? |
|---|---|---|---|---|
| Q-037-1 | Расширение allowlist `context`: доп. ключи (помимо `codeLanguage`/`responseStyle`/`verbosity`/`tone`/`locale`), перевод свободных строк (`codeLanguage`/`tone`) в enum, i18n служебного блока. | Open | Старт ([ADR-037 §1](adr/ADR-037-chatrunrequest-context-allowlist-injection.md)) — 5 ключей, реестр в коде; неизвестные ключи игнорируются (forward-compat). Расширение — аддитивно, без слома контракта. | Нет (реализуемо на текущем наборе) |

> Связь `context` (per-message override) с серверными дефолтами `preferences.code_defaults` (как взаимодействуют, кто главнее) — **вне scope ADR-037**, зафиксировано как [TD-028](100-known-tech-debt.md).

## Открытые вопросы — перенос чата в воркспейс (2026-06-18, [ADR-038](adr/ADR-038-move-chat-to-workspace.md))

| ID | Вопрос | Статус | Принятый дефолт (если есть) | Блокирует backend? |
|---|---|---|---|---|
| Q-038-1 | Ретроактивная подача **файлов-знаний** перенесённому чату: при переносе обычного чата в воркспейс (`PATCH /v1/chats/{id}` с `workspaceProjectId`) подмешивать ли файлы проекта на следующем ходе (вариант b), или оставить файлы доступными только чатам, созданным в воркспейсе (вариант a)? | **Closed на старте (вариант a)** | [ADR-038 §3.2](adr/ADR-038-move-chat-to-workspace.md): **вариант a** — файлы НЕ переинъектируются ретроспективно (turn-0-only), применяются только `instructions`. Обоснование: стоимость токенов, контекст-окно, нестабильность prompt-cache, симметрия с inline-attachments ([ADR-020](adr/ADR-020-inline-base64-attachments-mvp.md)). Вариант b (реинъекция файлов на первом ходе после переноса) — возможное будущее усиление, аддитивно. | Нет (реализуемо на варианте a) |

> Q-038-* (детали): API-форма (единый `PATCH` vs выделенный `POST`), код ошибки целевого workspace (`404` vs `422`), session-fixed vs изменяемость, аудит-событие, инъекция instructions на каждом ходе — **закрыты решениями [ADR-038](adr/ADR-038-move-chat-to-workspace.md)**, не заводятся как открытые вопросы.

## Открытые вопросы — редактирование сообщения (2026-06-19, [ADR-040](adr/ADR-040-edit-message-and-regenerate.md))

| ID | Вопрос | Статус | Принятый дефолт (если есть) | Блокирует backend? |
|---|---|---|---|---|
| Q-040-1 | Standalone-удаление сообщения **без** перегенерации (удалить ход и оставить чат на предыдущем состоянии, без нового `/chat/run`) — отдельный endpoint? | **Open (вне scope ADR-040)** | ADR-040 покрывает только edit+regenerate одним вызовом `/chat/run` (`editMessageStepId`). Чистое удаление сообщения без регенерации — отдельная фича; при необходимости — отдельный endpoint/ADR. | Нет |
| Q-040-2 | Refund-policy при редактировании: возвращать ли кредит за усечённый старый ход? | **Closed на старте (no-refund)** | [ADR-040 §3](adr/ADR-040-edit-message-and-regenerate.md): **no-refund-on-edit** — регенерация тарифицируется как новый ход (новый дебит 1 кредита), возврата за удалённый ход нет. Обоснование: append-only ledger ([ADR-005](adr/ADR-005-idempotency-ledger.md)), анти-абуз edit-цикла. Пересмотр — аддитивно. | Нет |
| Q-040-3 | Реинъекция **workspace-файлов** при редактировании ПЕРВОГО сообщения чата (сессия становится пустой, `is_new=False` → файлы turn-0-only не подаются). | **Closed на старте (вариант a)** | [ADR-040 §4а](adr/ADR-040-edit-message-and-regenerate.md): файлы НЕ реинъектируются (turn-0-only, симметрия с [ADR-038 Q-038-1](adr/ADR-038-move-chat-to-workspace.md)); `instructions` подаются как обычно. Та же ось, что Q-038-1. | Нет |
| Q-040-4 | Откатывать ли усечение истории, если **отредактированный** ход блокируется policy (`status=blocked`) на этапе policy-чека (история уже усечена до policy, как и user-шаг персистится до policy в обычном потоке)? | **Open (на старте — не откатывать, established-поведение)** | На старте усечение не откатывается при policy-block: ADR-040 трактует edit как обычный ход, а в обычном потоке user-шаг персистится до policy-чека ([ADR-002](adr/ADR-002-access-policy-state-machine.md)). Следствие: при policy-block отредактированного хода старая история (усечённая) теряется без новой генерации. Согласуется с established-поведением; пересмотр (откат усечения при block) — аддитивно, если окажется UX-проблемой. | Нет |

## Открытые вопросы Sign in with Apple (2026-06-19, [ADR-043](adr/ADR-043-sign-in-with-apple.md))

| ID | Вопрос | Статус | Принятый дефолт (если есть) | Блокирует backend? |
|---|---|---|---|---|
| Q-043-1 | Ужесточение Apple-входа: обязательный nonce + anti-replay store; обновление `email` при повторных входах; поддержка Services ID (web-flow аудитория, отдельная от bundle id); доп. провайдеры в `auth_identities` (email/Google). | Open (отложено) | Старт ([ADR-043 §2,§3,§5](adr/ADR-043-sign-in-with-apple.md)): nonce опционален (проверяется при наличии обеих сторон); `email` пишется при создании identity, на резолве не перезаписывается; аудитория = bundle id (нативный flow), Services ID не поддерживается; провайдер только `apple`. Расширения аддитивны (`auth_identities.provider` уже параметризован). | Нет (реализуемо на дефолте) |
| Q-043-2 | Авто-merge данных (кошелёк/история/ledger) при конфликте `apple_sub`-user ≠ device-user (вход в свой Apple-аккаунт на устройстве, уже привязанном к другому `userId`). | Open (отложено) | Старт ([ADR-043 §5](adr/ADR-043-sign-in-with-apple.md)): merge **НЕ** выполняется — берём `apple_sub`-user, устройство upsert на него; прежний device-аккаунт остаётся доступен по своему refresh-token. Авто-merge — продуктовый риск (двойное начисление, конфликт append-only ledger [ADR-005](adr/ADR-005-idempotency-ledger.md)); вводится отдельным ADR при подтверждённой потребности. | Нет (реализуемо без merge) |

## Открытые вопросы Hermes-интеграции (2026-06-23, [ADR-044](adr/ADR-044-client-api-key-auth.md)..[ADR-048](adr/ADR-048-admin-credits-and-subscription-grant.md))

| ID | Вопрос | Статус | Принятый дефолт (если есть) | Блокирует backend? |
|---|---|---|---|---|
| Q-044-1 | Усиление модели доверия к `X-User-Id`: подпись идентичности (HMAC под клиентским ключом), per-client ключи с атрибуцией, mTLS вместо единого `CLIENT_API_KEY`. | Open (отложено) | Старт ([ADR-044 §3](adr/ADR-044-client-api-key-auth.md)): единый доверенный `CLIENT_API_KEY` + ротация `CLIENT_API_KEY_PREV`; identity из `X-User-Id` доверяется. Усиление аддитивно (не ломает контракт). Реализуемо на дефолте. | Нет |
| Q-046-1 | Стратегия масштабирования per-user runtime при тысячах активных пользователей (per-user контейнеры vs пулы vs serverless-бэкенд Modal/Daytona). | Open (отложено) | Старт ([ADR-046](adr/ADR-046-per-user-hermes-runtime.md)): per-user Docker-контейнер + гибернация (`stop_idle`). `RuntimeBackend` расширяем — миграция на пул/serverless без переписывания manager/registry. Реализуемо на дефолте. | Нет |
| Q-046-2 | Политика хранения/удаления тома `HERMES_HOME` неактивного пользователя (когда `deprovision` удаляет данные, retention неактивных). | Open | Старт ([ADR-046 §1](adr/ADR-046-per-user-hermes-runtime.md)): `stop_idle` только останавливает контейнер, **том сохраняется** (память/навыки не теряются); `deprovision` удаляет контейнер, том — по явной политике (на старте не удаляется автоматически). Авто-очистка томов давно неактивных — будущее (как [TD-010](100-known-tech-debt.md)/[TD-013](100-known-tech-debt.md), при появлении планировщика). | Нет |
| Q-046-3 | Корректность дефолта `HERMES_VOLUME_ROOT=/opt/data/hermes` при мульти-инстанс control plane на shared-сервере ([ADR-017](adr/ADR-017-shared-server-traefik-deploy.md)): нужен ли инстанс-префикс в пути/именах томов, чтобы тома соседних control-plane-инстансов на общем Docker daemon не коллизировали (как `hermes-user-<id>` уже учитывает инстанс — см. [07-deployment.md §Мульти-инстанс](07-deployment.md#мульти-инстанс--клонирование-сервиса)). | Open | Дефолт backend (Спринт 2): `HERMES_VOLUME_ROOT=/opt/data/hermes` — корректен для одиночного инстанса. При мульти-инстанс задаётся **per-instance** в env (напр. `/opt/data/<proj>/hermes`) во избежание коллизий; имена Hermes-контейнеров уже несут инстанс-учёт ([07-deployment.md](07-deployment.md)). Код не меняется — фиксируется операционно/backend'ом при включении мульти-инстанс. | Нет |
| Q-046-4 | Патченый образ Hermes ([ADR-065](adr/ADR-065-patched-hermes-image-ghcr.md)): (а) **visibility ghcr-пакета** `ghcr.io/eliseiv/hermes-agent` — публичный (анонимный pull на `.156`, нет секрета) vs приватный (`docker login read:packages` под root); (б) **upstream-база патча** — какому upstream-commit/release соответствует рабочая копия `D:\BA\hermes` (нужно для чистого `git apply` `infra/hermes-patches/usage-delta.patch`, Вариант B). | Open (не блокер — есть дефолты) | Дефолты ([ADR-065](adr/ADR-065-patched-hermes-image-ghcr.md)): (а) **публичный пакет** (MIT позволяет распространение, минимум секретов; при требовании приватности — переключить на private + `docker login`); (б) при неопределённой/недоступной upstream-базе — **фолбэк A** (snapshot текущего дерева `D:\BA\hermes` как самостоятельный build-source, [ADR-065 Alt-1](adr/ADR-065-patched-hermes-image-ghcr.md)). Реализация не блокируется — devops берёт дефолты; оператор подтверждает visibility и (при Варианте B) пиннит upstream-базу. Sync-долг — [TD-036](100-known-tech-debt.md). | Нет |
| Q-047-1 | Точные значения `CREDITS_PER_1K_INPUT`/`CREDITS_PER_1K_OUTPUT` и политика округления (ceil/round, минимум за прогон). | Open | Дефолт ([ADR-047 §2](adr/ADR-047-usage-based-billing-for-agent.md)): коэффициенты конфигурируемы (env); округление **вверх (`ceil`)** до целого кредита, минимум `1` при ненулевом usage (кредиты целые, [03-data-model.md](03-data-model.md)). Конкретные числовые значения коэффициентов — задаёт оператор по себестоимости провайдера до приёма трафика. Реализуемо на дефолте. | Нет (калибровка значений — операционная) |
| Q-047-2 | Поведение при разрыве SSL до `run.completed` (нет debit на этом соединении) и при usage больше остатка баланса (нет pre-debit hold). Нужна ли реконсиляция/hold? | **Closed (2026-07-29, [ADR-067](adr/ADR-067-agent-run-background-consumer.md))** — ранее «Частично Closed (2026-06-24, [ADR-051](adr/ADR-051-agent-debt-reconciliation.md))» | **Часть 1 — реконсиляция несписанной дельты: решена** ([ADR-051](adr/ADR-051-agent-debt-reconciliation.md), закрывает [TD-029](100-known-tech-debt.md)): недобор → `wallets.debt`, clawback при `grant`, policy-блок `debt_outstanding`. Pre-debit hold отвергнут. **Часть 2 — прогон без подписчика: закрыта [ADR-067](adr/ADR-067-agent-run-background-consumer.md).** **Измеренный факт (наша БД, надёжно):** 3 реальных прогона 3 пользователей (usage `6294/56`, `6306/883`, третий) → **0 списаний кредитов**, `trialRemaining`=1 у всех троих, прогон #1 в `running` >15 мин после фактического завершения. ⚠️ **Прежняя митигация «повторная подписка на `/events` довыполнит debit» негодна** — но по причине, не требующей никаких допущений об образе: она предполагает, что клиент переподключится, а измеренный факт показывает, что он этого не делает. Дополнительно есть **неподтверждённая** гипотеза H1 ([Q-066-1](99-open-questions.md)), что переподписка вообще получает 0 байт; **на закрытие вопроса она не влияет** — гарантию биллинга нельзя строить на действии клиента ни при каком исходе H1. **Решение ([ADR-067](adr/ADR-067-agent-run-background-consumer.md) §0–§2):** broker — единственный upstream-подписчик Hermes — наш consumer, стартующий при `POST /v1/agent/run`/`/resume` до отдачи `202`; он исполняет биллинг, снапшот-writer и `_mark_terminal` **независимо от присутствия клиента**. Страховка — orphan-reaper (§5) с heartbeat в `agent_runs.updated_at` и защитой от массовой ложной финализации при перезапуске Redis. **До реализации backend пробел живёт как [TD-037](100-known-tech-debt.md)** (severity **high**, влияет на выручку). ⚠️ **Остаточная условность:** consumer закрывает вопрос в happy path полностью, но **страховочная** ветка (orphan-reaper при потере consumer'а) тарифицирует лишь по последнему наблюдённому кумулятиву, а при `agent_incremental_billing_enabled=false` — из снапшота вообще нечем; полнота этой ветки зависит от [Q-067-4](99-open-questions.md) (несёт ли hydrate токены, гипотеза H3). То есть «закрыт» относится к основному пути; хвост при аварийной потере consumer'а может недобираться. | Нет (решение принято; реализация — отдельная задача backend) |
| Q-047-3 | BYOK для агентного пути: давать ли пользователю подавать свой LLM-ключ в Hermes-инстанс (`mode=byok` для `/v1/agent/*`)? | Open (отложено) | Старт ([ADR-047 §3](adr/ADR-047-usage-based-billing-for-agent.md)): агент использует СЕРВИСНЫЙ `LLM_PROVIDER`/ключ инстанса ([ADR-046](adr/ADR-046-per-user-hermes-runtime.md)); policy `/v1/agent/*` работает в `credits`-ветке. BYOK агента (per-instance пользовательский ключ в env инстанса) — будущее, отдельным ADR. | Нет |
| Q-048-1 | Ретировать ли старый путь `POST /v1/admin/wallet/grant` после ввода `POST /v1/admin/credits/grant` (алиас vs удаление)? | Open | Старт ([ADR-048 §1](adr/ADR-048-admin-credits-and-subscription-grant.md)): новый путь `credits/grant`, старый `wallet/grant` — алиас на переходный период (обратная совместимость), контракт тела/ответа идентичен. Удаление алиаса — после подтверждения миграции операторских инструментов. | Нет |
| Q-053-1 | Распространять ли durable append-only-защиту ([ADR-053](adr/ADR-053-audit-logs-db-append-only.md): REVOKE + триггер) на `ledger_transactions`; нужен ли hash-chain / внешний WORM-storage (tamper-evidence) и формальная политика erasure (GDPR) для `audit_logs`? | Open (отложено) | Старт ([ADR-053 §3](adr/ADR-053-audit-logs-db-append-only.md)): защита применяется **только** к `audit_logs`; `ledger_transactions` защищён CHECK-инвариантами + idempotency (UPDATE/DELETE в коде нет). Hash-chaining/WORM и формальная erasure-процедура — будущее усиление при ужесточении комплаенса. Реализуемо на старте без них. | Нет |
| Q-054-1 | Нужен ли автоматический watchdog для «потерянного trial» (claim выполнен §1, но reconcile §2 не отработал из-за краша в окне claim↔generate↔reconcile → `trial_used=TRUE` без выданной бесплатной генерации)? | Open (отложено) | Старт ([ADR-054 §4](adr/ADR-054-trial-claim-reconcile.md)): без кода — редкий кейс, направлен в сторону сервиса (анти-абуз), митигируется операционно (оператор возвращает trial по обращению). Автоматический reconcile «claimed но нет успешного assistant_message за TTL» — будущее усиление при заметной частоте. | Нет (реализуемо без watchdog) |

## Открытые вопросы — конфигурация LLM Hermes-инстанса (2026-06-24, [ADR-055](adr/ADR-055-hermes-instance-llm-config-contract.md))

| ID | Вопрос | Статус | Принятый дефолт (если есть) | Блокирует backend? |
|---|---|---|---|---|
| Q-055-1 | Точное имя env-переменной ключа (или канал передачи ключа) для провайдера `custom` в текущей версии образа Hermes: вводит ли образ `CUSTOM_API_KEY`, либо ключ для `custom` передаётся через `config.yaml model.api_key`? | **Resolved** ([ADR-055 §6](adr/ADR-055-hermes-instance-llm-config-contract.md)) | Подтверждено чтением образа: `custom` объявлен `env_vars=()` (`plugins/model-providers/custom/__init__.py:65`) — `CUSTOM_API_KEY` образ **не** вводит и **игнорирует**; единственный канал ключа для `custom` — `config.yaml model.api_key` (`hermes_cli/config.py:3991`). Решение: набор `HERMES_PROVIDERS_CONFIG_API_KEY = {custom}`; для них `render_instance_config` эмитит `model.api_key="${HERMES_INSTANCE_LLM_KEY}"` (env-ссылка — плейнтекст НЕ пишется в том, образ раскрывает `${}` при загрузке config, `config.py:5531`), а `manager` передаёт env `HERMES_INSTANCE_LLM_KEY=<ключ>` вместо бесполезного `<PROVIDER>_API_KEY`. | Нет (решение зафиксировано; backend реализует §6) |
| Q-055-2 | Поддержание map провайдер→key-env в синхроне с образом Hermes при апгрейде (новые/переименованные провайдеры в `cli-config.yaml.example`). | Open (операционное) | Старт ([ADR-055 §4](adr/ADR-055-hermes-instance-llm-config-contract.md)): map в коде — источник `cli-config.yaml.example`/`.env.example` образа на момент pin-тега `HERMES_IMAGE`. При апгрейде образа backend сверяет map; неизвестный провайдер уже падает fail-fast (allowlist), что сигнализирует расхождение. | Нет |

## Открытые вопросы — readiness-gate и владение томом (2026-06-24, [ADR-056](adr/ADR-056-provision-readiness-gate-and-volume-ownership.md))

| ID | Вопрос | Статус | Принятый дефолт (если есть) | Блокирует backend? |
|---|---|---|---|---|
| Q-056-1 | Делать ли `HERMES_PROVISIONING_STALE_SECONDS` производным от `HERMES_PROVISION_READY_TIMEOUT_SECONDS` (напр. `stale = ready + margin`) вместо двух независимых env с fail-fast-инвариантом `stale > ready`? | Open (отложено) | Старт ([ADR-056 §3](adr/ADR-056-provision-readiness-gate-and-volume-ownership.md)): два независимых env (`stale=120`, `ready=90`), инвариант `stale > ready` валидируется fail-fast в `config.py`. Производный расчёт — упрощение конфигурации, не функциональное требование. Реализуемо на дефолте. | Нет |
| Q-056-2 | Выводить ли `HERMES_UID`/`HERMES_GID` из единого источника с uid/gid api-контейнера (`docker-compose`), чтобы исключить рассинхрон конфигов (compose vs env → возврат дефекта B)? | Open (операционное) | Старт ([ADR-056 §4](adr/ADR-056-provision-readiness-gate-and-volume-ownership.md)): `HERMES_UID`/`HERMES_GID` — отдельные env с дефолтом `10001` (= uid/gid api по умолчанию); операционное требование держать их синхронными с `api`-сервисом в compose. Единый источник (вывод из uid процесса в рантайме или общий compose-var) — будущее упрощение; рантайм-интроспекция отвергнута ради детерминизма/тестируемости. Реализуемо на дефолте. | Нет |

## Открытые вопросы — выделенный сервер + self-hosted Traefik (2026-06-25, [ADR-057](adr/ADR-057-dedicated-server-self-hosted-traefik-deploy.md))

| ID | Вопрос | Статус | Принятый дефолт (если есть) | Блокирует backend? |
|---|---|---|---|---|
| Q-057-1 | Как разрешить двойственность `docker-compose.prod.yml` для двух deploy-target-ов: broadnova `.154` (внешний Traefik, без сервиса `traefik`, `web` = `external`) vs avorelio `.156` (наш сервис `traefik`, `web` = internal compose) — единый файл с профилем/override ИЛИ отдельный prod-файл для `.156`? | **Closed (2026-06-25, devops-реализация, [ADR-057 §3](adr/ADR-057-dedicated-server-self-hosted-traefik-deploy.md))** | **Отдельный overlay-файл `.156`** (напр. `docker-compose.traefik.yml`, применяется `-f docker-compose.prod.yml -f docker-compose.traefik.yml`): добавляет сервис `traefik` + acme named volume и **переопределяет сеть `web` на внутреннюю явным `networks.web.external: false`**. Эмпирически (Compose v5.0.2): пустой `web: {}` **НЕ срабатывает** — наследует `external:true` базового файла; требуется именно `external: false`. Базовый `docker-compose.prod.yml` не трогается → broadnova `.154` (деплой без overlay) не регрессирует (инвариант ADR-017 §12). Код не меняется (только инфра). | Нет (Closed) |
| Q-057-2 | Нужен ли socket-proxy (allowlist Docker API, read-only) перед docker.sock для сервиса `traefik` уже на старте, или достаточно `:ro` + выделенный сервер с переходом на socket-proxy при ужесточении? | Open (операционное) | Старт ([ADR-057 §4](adr/ADR-057-dedicated-server-self-hosted-traefik-deploy.md)): `docker.sock :ro` напрямую в `traefik` (provider) на выделенном сервере; socket-proxy (`tecnativa/docker-socket-proxy` с `CONTAINERS=1`/`NETWORKS=1` read-only) — **задел**, добавляется без изменения контракта при ужесточении. Реализуемо на дефолте. | Нет |
| Q-061-1 | Нужен ли отдельный admin-эндпоинт коррекции **самого долга** (`wallets.debt`, [ADR-051](adr/ADR-051-agent-debt-reconciliation.md)) — списать/обнулить/выставить долг оператором — помимо `wallet/debit` (который трогает только `balance`)? | Open (отложено) | Старт ([ADR-061 §5](adr/ADR-061-admin-wallet-debit.md)): `wallet/debit` корректирует **только** `balance` (`meta.source="admin_debit"` не запускает реконсиляцию долга); `debt` гасится штатно clawback'ом при `credits/grant` (оператор пополняет → долг списывается). Прямой admin-adjust долга — отдельным ADR при подтверждённой потребности; на старте не требуется (долг закрывается grant'ом). | Нет |
| Q-062-1 | Достаточен ли `GET /health` как readiness-сигнал `api_server` инстанса Hermes, или нужен более точный ready-probe (`/v1/health`, `/health/detailed`, пробный `POST/GET /v1/runs`)? | Open (отложено) | Старт ([ADR-062](adr/ADR-062-wake-readiness-gate-and-connect-only-launch-retry.md)): `/health` достаточен — маршруты образа (`/health`+`/v1/runs`) на одном aiohttp-listener, регистрируются ДО `TCPSite.start()` → `/health=200` ⟺ `/v1/runs` маршрутизируем; прод-`502` был transport-ошибкой (connect-фаза), а не `5xx` готового хендлера. Пересмотр **только если** после фикса появятся post-gate `5xx` (не transport-ошибки), указывающие на зависимость `/v1/runs` от async-warmup после старта listener. Реализуемо на дефолте. | Нет |

## Открытые вопросы — agent-run: биллинг, снапшот, фоновый consumer ([ADR-064](adr/ADR-064-incremental-agent-run-billing-and-pause-resume.md) 2026-07-15, [ADR-066](adr/ADR-066-agent-run-state-snapshot.md) 2026-07-28, [ADR-067](adr/ADR-067-agent-run-background-consumer.md) 2026-07-29)

| ID | Вопрос | Статус | Принятый дефолт (если есть) | Блокирует backend? |
|---|---|---|---|---|
| Q-064-1 | Реконсиляция при потере `usage.delta` на реконнекте (шаг с усечённой доставкой между `last_billed_step` и обрывом SSE): дозачитывать ли пропущенный шаг? | Open | Старт ([ADR-064 §2,§6](adr/ADR-064-incremental-agent-run-billing-and-pause-resume.md)): seed `charged` из ledger (`charged_for_run`) + per-step idempotency (`runId:step`) защищают от **двойного** счёта; финализация остатка на `run.completed` (`remainder=usage_to_credits(final)−charged`) **добирает** пропущенное при нормальном завершении. Активная дореконсиляция «зависших» без `run.completed` (по `last_billed_step`+`cumulative_credits_spent`) — будущее усиление. Реализуемо на дефолте. | Нет |
| Q-064-2 | Частичный шаг на границе паузы: как трактовать недобор `(want − balance)` последнего шага (сервис поглощает ≤ одного шага vs долг vs частичный billing суб-шага)? | Open | Старт ([ADR-064 §3](adr/ADR-064-incremental-agent-run-billing-and-pause-resume.md)): списывается `min(want,balance)`, недобор ≤ дельты одного `usage.delta` **поглощается** сервисом на границе `stop` (НЕ долг — суть pause-at-zero). Гранулярность защиты = один LLM-вызов loop. Конвертация недобора в долг/суб-step billing — отвергнута (возврат к debt-модели [ADR-051](adr/ADR-051-agent-debt-reconciliation.md)). Реализуемо на дефолте. | Нет |
| Q-064-3 | Hydrate на resume при истёкшей/пустой Hermes-сессии (`GET {base}/api/sessions/{session_id}/messages` вернул `404`/пусто): поведение? | Open | Старт ([ADR-064 §5,§7](adr/ADR-064-incremental-agent-run-billing-and-pause-resume.md)): при недоступной сессии hydrate → `409` (session_expired) — resume невозможен, клиент начинает новый прогон (`POST /v1/agent/run`). Точную форму подтвердить при патче образа Hermes (наличие/контракт endpoint). Не блокирует основной путь. | Нет (подтвердить при Hermes-патче) |
| Q-064-4 | Монотонность `usage.delta.step_index` (`session_api_calls`) **и допустимый charset Hermes `run_id`**: гарантирует ли образ Hermes строго возрастающий не повторяющийся `step_index` (иначе коллизия ключа `runId:step`)? Содержит ли `run_id` LIKE-метасимволы (`%`/`_`) — влияет на seed `charged_for_run` ([ADR-064 §6](adr/ADR-064-incremental-agent-run-billing-and-pause-resume.md))? | **Частично Closed (2026-07-29, замер прода) — charset подтверждён; монотонность `step_index` остаётся Open** | **Charset — подтверждён на реальном образе ([ADR-065](adr/ADR-065-patched-hermes-image-ghcr.md)):** формат `run_id` = **`run_` + 32 hex** (см. [Q-066-2](99-open-questions.md)). ⚠️ **Экранирование в seed-запросе снимать НЕЛЬЗЯ:** hex-часть метасимволов не содержит, но **сам префикс `run_` содержит `_`** — а `_` в `LIKE` есть wildcard «любой один символ». Практического эффекта нет (все `run_id` имеют одинаковый префикс, поэтому wildcard в позиции 4 не может дать ложное совпадение с другим прогоном), но `LIKE … ESCAPE` с экранированием `%`/`_`/обратного слэша ([ADR-064 §6](adr/ADR-064-incremental-agent-run-billing-and-pause-resume.md)) остаётся **обязательным** — при любом будущем изменении формата префикса незаэкранированный `_` немедленно станет дефектом биллинга. **`step_index` существует и нумеруется с 1** (прод-капча `tests/fixtures/hermes_prod_run_adr065.sse`: `step_index: 1`, состав `usage.delta` совпадает с [ADR-064 §7](adr/ADR-064-incremental-agent-run-billing-and-pause-resume.md)). **Монотонность по-прежнему НЕ проверена — в капче один шаг, последовательности нет:** прод-прогоны 2026-07-28/29 списаний не произвели вовсе ([Q-047-2](99-open-questions.md)), поэтому наблюдать последовательность шагов было не на чем. Проверяется первым же прогоном после [ADR-067](adr/ADR-067-agent-run-background-consumer.md) при `agent_incremental_billing_enabled=true`. Дефолт прежний: предполагается монотонный счётчик API-вызовов loop; при немонотонности — дополнить ключ (`runId:step:cumTotal`) или взять кумулятив как якорь. **Tenancy-гвард снапшота** — [Q-066-2](99-open-questions.md) (Closed). | Нет (проверить первым incremental-прогоном после ADR-067) |
| Q-064-5 | BYOK для incremental agent-пути (пользователь подаёт свой LLM-ключ в инстанс): применима ли pause-at-zero, когда токены оплачивает пользовательский ключ, а не кредиты сервиса? | Open (defer) | Отложено (как [Q-047-3](#открытые-вопросы-hermes-интеграции-2026-06-23-adr-044adr-048)): агент на старте использует СЕРВИСНЫЙ ключ инстанса ([ADR-046](adr/ADR-046-per-user-hermes-runtime.md)), биллинг в кредитах. BYOK-агент (свой ключ) меняет экономику pause-at-zero — проектируется отдельным ADR при вводе BYOK для `/v1/agent/*`. | Нет |
| Q-066-1 | **Семантика буфера событий Hermes при подписке на `GET /v1/runs/{id}/events`.** Исходная формулировка была **бинарной** (реплей с начала vs только новые) и оказалась неполной. | **Partially Closed (2026-07-29)** — закрыто только **сырое наблюдение**; интерпретация (дренаж) остаётся **Open** до перемера с критерием живости | **Что установлено (наблюдение):** после обрыва подписки A вторая **последовательная** подписка B на тот же `runId` получила **0 байт**, `run.completed` в B не пришёл. ⚠️ **Что НЕ установлено:** что это дренаж буфера. **Конфаундер — живость прогона в момент B не зафиксирована:** если прогон к открытию B уже завершился, «0 байт» объясняется тривиально (эмитить было нечего) и о повторном реплее не говорит ничего. Сырые артефакты замера (`runId`, тайминги открытия/обрыва A, момент открытия B, счётчики байт, момент фактического завершения прогона) **не сохранены**, апостериорно различить два объяснения нельзя. Гипотеза зафиксирована как **H1** в [ADR-067 §Непроверенные внешние допущения](adr/ADR-067-agent-run-background-consumer.md). ⚠️ **Дискриминатор старой процедуры (`char_length(resultText)`) был непригоден** независимо от конфаундера: из-за дефекта парсинга `message.delta` (поле `delta` несёт **значение-строку**, а не объект с `.text` — [05-events.md](modules/agent-proxy/05-events.md)) `resultText` был тождественно пуст, и обе ветки таблицы исходов давали «текст замер». ⚠️ **Реплей первому подписчику (H4) процедурой не упражнялся вовсе:** A подключалась сразу после старта прогона, реплеить было нечего. Утверждение [ADR-066 §6](adr/ADR-066-agent-run-state-snapshot.md) «Hermes реплеит буфер с начала» остаётся **непроверенным допущением**, а не фактом. **Перемер (owner: qa, после фикса парсинга; процедура — [09-testing.md](modules/agent-proxy/09-testing.md)).** ✅ **Дискриминатор живости теперь есть и он прямой:** события образа несут **`timestamp` (float, epoch-секунды)** — подтверждено прод-капчей `tests/fixtures/hermes_prod_run_adr065.sse` ([05-events.md](modules/agent-proxy/05-events.md)); достаточно, чтобы B получил событие с `timestamp` позже момента обрыва A. Обязательный **критерий живости** — вывод о дренаже допустим ТОЛЬКО если B получил хотя бы одно событие с меткой позже момента обрыва A, либо прогон доказуемо продолжался во время B (рост `usage`/`lastTool` в `/state`, либо пришедшее позже `run.completed`). Сохранять сырые артефакты. Отдельно проверить H4 подпиской, открытой заведомо позже первых событий. **На решение [ADR-067](adr/ADR-067-agent-run-background-consumer.md) исход НЕ влияет** ([ADR-067 §0](adr/ADR-067-agent-run-background-consumer.md)): broker обоснован измеренным фактом «0 списаний из 3» и топологией 4 воркеров. Исход влияет на adoption/ретрай ([Q-067-1](99-open-questions.md)) и на оценку альтернативы 2. | Нет |
| Q-066-2 | **Глобальная уникальность Hermes `run_id`:** `run_id` генерирует **инстанс каждого пользователя независимо**, а `agent_runs.run_id` — **глобальный `TEXT PRIMARY KEY`** ([03-data-model.md §24](03-data-model.md)). Каков реальный формат/энтропия `run_id` в образе и гарантирована ли неколлизионность между инстансами разных пользователей? | **Closed (2026-07-29, замер прода на образе [ADR-065](adr/ADR-065-patched-hermes-image-ghcr.md))** | **Формат подтверждён первоисточником — прод-капчей потока самого образа** (`tests/fixtures/hermes_prod_run_adr065.sse`, прогон `run_d931839587a64e3885b4d096cf7440d0`): **`run_` + 32 hex** ⇒ **~128 бит энтропии**. Допущение control plane о глобальной уникальности `run_id` (PK без префикса `user_id`) **валидно**: при 128 битах вероятность коллизии по birthday-оценке пренебрежима на любом обозримом масштабе (иллюстративный `run_7f3c1a` из старых примеров docs — 6 hex — к реальному образу отношения не имел). **Составной ключ `(user_id, run_id)` и префиксование `run_id` не нужны, миграция не заводится.** **Tenancy-гвард остаётся** — но как defense-in-depth, а не как митигация коллизии: `WHERE t.user_id = EXCLUDED.user_id` в апсерте снапшота, owner-scoped `snapshots_repo.get(run_id, user_id)`, `clear_pending_approval(run_id, user_id)`, `mark_stopped(run_id, user_id)`, RBAC-404 ([06-rbac.md](modules/agent-proxy/06-rbac.md)) — они дёшевы, делают RBAC свойством самого запроса и защищают от ошибки в вызывающем коде, а не только от коллизии. Снимать их **запрещено**. ⚠️ **LIKE-экранирование в seed `charged_for_run` снимать НЕЛЬЗЯ** несмотря на «безопасный» charset: префикс `run_` содержит `_` — wildcard `LIKE` (детали — [Q-064-4](99-open-questions.md)). | Нет |
| Q-066-3 | **Обратная ссылка parent→child в resume-цепочке (`continuedTo`):** вводить ли аддитивное поле в `GET /v1/agent/runs/{runId}/state`? После `/resume` родительская строка навсегда `resumed` → клиентский `running` с замороженным `updatedAt`; клиент, потерявший `runId` продолжения (kill приложения сразу после `/resume`), опрашивая **родительский** `runId`, не имеет из ответа пути к активному child (в v1 есть только child→parent `continuedFrom`). | Open | **Дефолт v1 — без поля** ([ADR-066 §4](adr/ADR-066-agent-run-state-snapshot.md)): рабочий обходной путь есть и он идемпотентен — повторный `POST …/resume` резолвит `active_child` и возвращает `runId` продолжения, второй child не создаётся ([ADR-064 §5](adr/ADR-064-incremental-agent-run-billing-and-pause-resume.md)). Ограничение донесено iOS ([modules/agent-proxy/02-api-contracts.md §Известные ограничения](modules/agent-proxy/02-api-contracts.md)). **Не tech-debt:** контракт работоспособен, вопрос в удобстве клиента — поэтому Q, а не TD. Решать по фидбеку iOS: при подтверждённом неудобстве добавить `continuedTo: string \| null` (аддитивно, обратная совместимость) в следующей ревизии контракта. | Нет |
| Q-067-1 | **Доедут ли свежей подписке на `GET /v1/runs/{id}/events` события, произведённые ПОСЛЕ отключения предыдущего потребителя?** Т.е. если consumer умер mid-run (рестарт `api`, краш воркера, обрыв своей upstream-подписки), может ли другой воркер переподключиться и получить **остаток** живого прогона? От ответа зависят **две** отложенные возможности: подхват прогона (adoption) и ретрай собственной подписки consumer'ом ([ADR-067 §6.2](adr/ADR-067-agent-run-background-consumer.md)). | Open — **связан с H1** ([Q-066-1](99-open-questions.md)); прежний замер к этому вопросу неприменим (живость прогона в момент второй подписки не зафиксирована) | **Дефолт v1 — ни adoption, ни ретрая** ([ADR-067 §6.2/§9](adr/ADR-067-agent-run-background-consumer.md)): consumer при обрыве своей подписки выполняет самозавершение (флаш снапшота → снятие lease и heartbeat → audit) и **не переподключается**; осиротевший прогон добивает orphan-reaper. Причина запрета ретрая: при истинной H1 переподключение бесполезно и **вредно** (получит 0 байт, но будет удерживать lease/heartbeat и тем блокировать reaper), при ложной — даст повторный реплей с начала, безопасный для снапшота (префиксный guard), но ломающий дедуп по `seq` у подписчиков. **Процедура замера (owner: qa, вместе с перемером [Q-066-1](99-open-questions.md)):** запустить заведомо длинный прогон → дождаться событий у первого потребителя → оборвать его → подписаться заново → **зафиксировать живость** (событие с меткой позже обрыва, либо продолжение прогона по `/state`). Исход **«события идут»** ⇒ включить adoption (свип подхватывает подписку у прогонов без живого lease; ledger-идемпотентность `runId:step`/`runId` уже защищает от двойного списания) и ретрай с дедупом по `seq`; понизить `AGENT_RUN_ORPHAN_TIMEOUT_SECONDS`. Исход **«пусто при доказанной живости»** ⇒ reaper остаётся единственным путём, деплой под работающим прогоном навсегда стоит биллингового хвоста; рассмотреть drain-режим деплоя. **Не блокирует** реализацию [ADR-067](adr/ADR-067-agent-run-background-consumer.md): v1 спроектирован на пессимистичном исходе. | Нет |
| Q-067-2 | **Лимит одновременных активных agent-прогонов на пользователя.** Consumer держит upstream-соединение, задачу и **не даёт инстансу Hermes гибернироваться** на всё время прогона ([ADR-067 §6](adr/ADR-067-agent-run-background-consumer.md)) — ресурс, которого до broker-модели не существовало (уход клиента раньше «бесплатно» освобождал всё, ценой неоплаченного прогона). Нужен ли явный cap и на каком уровне (per-user / per-worker / глобальный)? | Open | **Дефолт v1 — без cap** ([ADR-067 §8](adr/ADR-067-agent-run-background-consumer.md)): естественные ограничители — policy-gate по кредитам/долгу до запуска ([ADR-002](adr/ADR-002-access-policy-state-machine.md)/[ADR-051](adr/ADR-051-agent-debt-reconciliation.md)) и один Hermes-инстанс на пользователя ([ADR-046](adr/ADR-046-per-user-hermes-runtime.md)). Мерить в проде: число одновременных consumer-задач и число инстансов, удерживаемых от гибернации. При проблеме — `AGENT_RUN_MAX_CONCURRENT_PER_USER` с отказом на `POST /v1/agent/run` (код ответа — часть решения: `429` vs `200 {status:blocked}` по [ADR-004](adr/ADR-004-blocked-http-200.md)). Ввод аддитивен, контракт не ломает. | Нет |
| Q-067-3 | **Представление маркера усечения replay-буфера в `/events`.** Сам маркер **вошёл в v1** ([ADR-067 §3.4](adr/ADR-067-agent-run-background-consumer.md)) — открыт только выбор формы. | **Частично решён (2026-07-29): маркер обязателен, форма — открыта** | ⚠️ **Первая редакция откладывала маркер целиком — отозвано.** Без него клиент, выполнивший сброс накопленного текста при реконнекте без курсора, заменил бы полный текст на **заведомо неполный** (ring обрезан по `AGENT_RUN_EVENT_BUFFER_MAX`/`..._MAX_BYTES`) и **не смог бы это заметить** — молчаливая порча UI. **Принято в v1:** при первом отданном `seq` больше `курсор + 1` поток предваряется `data: {"event":"run.truncated","run_id":"…","from_seq":N,"reason":"buffer_trimmed"}`; семантика — «начало недоступно, полный текст брать в `GET …/state.resultText`». Событие аддитивно, игнорирующий его клиент ведёт себя как сегодня. **Остаётся открытым:** отдельное событие (как сейчас) vs поле в будущем `run.snapshot`-событии — решать по фидбеку iOS, замена аддитивна. Актуальность маркера снижена введением курсора `Last-Event-ID` ([ADR-067 §3.4](adr/ADR-067-agent-run-background-consumer.md)): клиент с курсором сброс не делает и усечение его не задевает. | Нет |
| Q-067-4 | **Несёт ли hydrate `GET {base}/api/sessions/{sessionId}/messages` данные об usage/токенах?** Эндпоинт читает состояние сессии с **персистентного тома**, переживающего гибернацию ([ADR-046](adr/ADR-046-per-user-hermes-runtime.md)), то есть доступен и после потери in-memory реестра прогонов — в отличие от run-state-эндпоинта ([ADR-066 §Alternatives 2](adr/ADR-066-agent-run-state-snapshot.md)). Если несёт — появляется **постфактум-источник usage**, не зависящий от SSE-потока. | Open — форма ответа образа **не проверена** (гипотеза **H3**, [ADR-067 §Непроверенные внешние допущения](adr/ADR-067-agent-run-background-consumer.md)); [ADR-064 §7](adr/ADR-064-incremental-agent-run-billing-and-pause-resume.md) уже фиксирует «наличие/форму подтвердить при патче» | **Влияние на [ADR-067](adr/ADR-067-agent-run-background-consumer.md):** (а) если H3 ложна (hydrate несёт токены) — orphan-reaper сможет тарифицировать прогон **и при `agent_incremental_billing_enabled=false`**, и полнее (не только наблюдённый кумулятив), а утверждение «reaper списал бы ноль» ([ADR-067 §Alternatives 1](adr/ADR-067-agent-run-background-consumer.md)) станет неверным; (б) соотношение цена/эффект «broker vs reaper+hydrate» сместится в пользу второго **в части биллинга**. **Выбор §0 при этом не отменяется** — reaper+hydrate не решает отставание снапшота фоновых прогонов, задержку тарификации на `AGENT_RUN_ORPHAN_TIMEOUT_SECONDS`, невозможность живого стрима после реконнекта и статус `failed` вместо фактического исхода. **Процедура:** вызвать на реальном образе после завершённого прогона, зафиксировать JSON-схему ответа в [05-events.md](modules/agent-proxy/05-events.md). **Проверить ДО реализации Phase 9** — результат меняет объём [ADR-067 §5](adr/ADR-067-agent-run-background-consumer.md). | Нет (но проверить до Phase 9) |
| Q-067-5 | **Поддерживает ли образ Hermes ОДНОВРЕМЕННЫХ подписчиков на один `runId`** (`GET /v1/runs/{id}/events` из двух соединений параллельно)? Гипотеза **H2**. | Open — **не измерялось**: проведённая проба была последовательной (A → обрыв → B) и об одновременности не говорит ничего | **Зачем:** от ответа зависит оценка [ADR-067 §Alternatives 2](adr/ADR-067-agent-run-background-consumer.md) (параллельный фоновый consumer при живом клиентском relay). ⚠️ **Ранее в docs было записано «два потребителя физически исключают друг друга» — формулировка снята как недоказанная.** **На решение не влияет:** даже при благоприятной H2 параллельный consumer хуже broker'а по независимым причинам (два источника событий на прогон → нужен протокол владения биллингом и снапшотом; вторая подписка к инстансу на каждый прогон). **Процедура:** открыть две подписки на живой прогон одновременно, зафиксировать, получают ли обе поток и совпадает ли он. | Нет |
| Q-067-6 | **Шлёт ли образ Hermes keep-alive в SSE-потоке (comment-line `: …`) и какова максимальная наблюдаемая пауза между байтами на длинном tool-call?** Гипотеза **H6**. | Open — **не измерялось** | ⚠️ **Вопрос заведён после отзыва дефекта.** Первая редакция [ADR-067 §6](adr/ADR-067-agent-run-background-consumer.md) вводила idle-таймаут consumer'а по **доменным событиям** (600 с), опираясь на молчаливое допущение, что образ шлёт что-то чаще. Допущение не проверялось, а последствие тяжёлое: у **работающего** прогона consumer снял бы lease и heartbeat, reaper затарифицировал бы его по неполному кумулятиву и пометил `failed`, а клиентский стрим закрылся бы ([ADR-067 §3.3](adr/ADR-067-agent-run-background-consumer.md) п. 5). Для агента пауза >10 мин на длинном tool-call (сборка, долгий web-запрос, ожидание approval) — **штатна**. **Решение:** idle-таймаут удалён; детектор смерти upstream — **транспортный** (TCP keep-alive + отключённый read-timeout), молчание признаком смерти не является. **Процедура замера:** снять капчу прогона с длинным tool-call (по образцу `tests/fixtures/hermes_prod_run_adr065.sse`), зафиксировать наличие comment-line и **максимальную паузу между байтами**. **До измерения любой порог по молчанию запрещён** — включая read-timeout на стриме. Результат нужен, только если появится потребность в таком пороге; текущий дизайн без него обходится. | Нет |
| Q-067-7 | **Все ли типы событий образа несут `timestamp`?** Гипотеза **H5**. Прод-капча `tests/fixtures/hermes_prod_run_adr065.sse` подтверждает метку у **`message.delta`** и **`usage.delta`**; для `tool.started`/`tool.completed`, `approval.request`, `run.queued`/`running`/`completed`/`failed` — **экстраполяция** (капча снята с прогона без вызова инструментов и без штатного терминального события). | Open | **Почему важно:** на `timestamp` стоит **критерий живости** процедуры перемера [Q-066-1](99-open-questions.md) («B получил событие с меткой позже обрыва A»). Если метки нет у части типов — критерий сужается до дельт, а при их отсутствии в окне замера придётся опираться на резервные признаки живости ([09-testing.md](modules/agent-proxy/09-testing.md): рост `usage`/`lastTool` в `/state`, позднее `run.completed` третьей подписке). **Процедура:** снять капчу прогона **с вызовом инструмента и штатным завершением**, зафиксировать наличие `timestamp` по каждому типу события; результат внести в [05-events.md](modules/agent-proxy/05-events.md). Синтетические события control plane (`run.paused`, `run.truncated`) метку не несут — это наш контракт, а не образа. | Нет |
| Q-067-8 | **Значение и продуктовая приемлемость `AGENT_RUN_MAX_DURATION_SECONDS`** (дефолт `7200` = 2 ч): «агентный прогон длиннее N принудительно прекращается». | Open — **требует продуктового решения**, дефолт действует до ответа | ⚠️ **Почему это не внутренняя настройка.** После отзыва idle-таймаута ([Q-067-6](99-open-questions.md)) это **единственный временной ограничитель**, и **вся верхняя граница жизни статуса `running`** ([ADR-067 §6.5](adr/ADR-067-agent-run-background-consumer.md)) выводится из него: `MAX_DURATION + ORPHAN_TIMEOUT +` тик reaper'а. При значении «без лимита» (`0`) граница **перестаёт существовать** — прогон под живым, но молчащим upstream может висеть неограниченно. Одновременно это **видимое пользователю** ограничение сервиса: прогон принудительно прекращается на середине работы, деньги за выполненную часть списываются, статус — `failed` ([ADR-067 §5](adr/ADR-067-agent-run-background-consumer.md)). **Нужно решить:** (а) приемлемы ли 2 часа продуктово (какова реальная верхняя длительность полезного агентного прогона); (б) как это донести до iOS и до пользователя (сообщение, отдельный `blockReason`, ничего); (в) допускается ли «без лимита» и что тогда с инвариантом §6.5. **Не блокирует** реализацию: дефолт задан и безопасен. Cross-ref: [ADR-067 §6.3/§6.5/§Consequences](adr/ADR-067-agent-run-background-consumer.md), [07-deployment.md](07-deployment.md). | Нет |
| Q-067-9 | **Доходит ли TCP keep-alive до контейнера инстанса через `hermes-net` и закрывает ли сокет при пропаже пира?** Гипотеза **H7**. | Open — **не измерялось** | **Почему критично:** после отзыва idle-таймаута keep-alive — **единственный** детектор смерти upstream ([ADR-067 §6.2](adr/ADR-067-agent-run-background-consumer.md)). ⚠️ **У `httpx` нет knob'а «включить keep-alive»** — нужны явные `socket_options` (`SO_KEEPALIVE` + `TCP_KEEPIDLE`/`TCP_KEEPINTVL`/`TCP_KEEPCNT`); без них действует Linux-дефолт `tcp_keepalive_time = 7200 с`, то есть детектор **фиктивен** (сработает позже собственного `MAX_DURATION`). Но даже с правильными опциями остаётся вопрос, не удерживает ли соединение промежуточный сетевой уровень Docker. **Процедура:** запустить прогон → `docker kill` контейнера инстанса → замерить, за какое время consumer получит ошибку чтения (ожидание — порядка `KEEPIDLE + KEEPINTVL × KEEPCNT` ≈ 90 с при дефолтах). **Исход «сокет не закрывается»** ⇒ детектор деградирует до `MAX_DURATION`, верхняя граница §6.5 становится равной ему, и вопрос [Q-067-8](99-open-questions.md) резко повышает приоритет. Cross-ref: [ADR-062](adr/ADR-062-wake-readiness-gate-and-connect-only-launch-retry.md) (прецедент требования транспортной конкретики). | Нет (проверить при E2E Phase 9) |
| Q-067-10 | **Форма `usage` в `run.completed` патченого образа не подтверждена НИ ОДНОЙ капчей.** Все имеющиеся прод-прогоны обрываются на синтетическом `run.paused` по исчерпанию баланса, поэтому штатное терминальное событие образа никто не наблюдал. Открытые варианты носителя: вложенный `{usage:{input_tokens, output_tokens, total_tokens}}` (как записано в docs с самого начала — **допущение, не наблюдение**) vs плоский верхний уровень (как у наблюдённого `usage.delta`), и в каждом — кумулятивные (`cumulative_*`) vs per-step имена. | Open — **владелец добычи артефакта: devops** (прод-прогон на пополненном кошельке до штатного завершения) **или qa** (контрактный тест против реального образа) | ⚠️ **Цена ошибки — прямая потеря выручки, и это не деградация UI.** При `agent_incremental_billing_enabled=false` (**дефолт**) списание, выведенное из `run.completed{usage}`, — **единственное за весь прогон** ([ADR-047 §2](adr/ADR-047-usage-based-billing-for-agent.md)); промах формы даёт `owed = 0`, что **неотличимо от легитимного нуля** и приводит к бесплатному прогону. Это тот же класс дефекта, что уже материализовался на `message.delta` (форма была придумана вместе с парсером — [05-events.md](modules/agent-proxy/05-events.md)), но на денежном пути. **Смягчение в коде (реализовано, риск НЕ закрывает):** `_extract_usage_counts` читает **union** носителей (`event.usage` и плоский верхний уровень) × наборов имён (`cumulative_*` / per-step), причём совпадения **не ранжируются, а сворачиваются поэлементным максимумом**: любое ранжирование отдаёт победу первому проверенному совпадению, даже нулевому (`{"usage":{6313,658},"cumulative_*":0}` тарифицировался бы в ноль), тогда как максимум безопасен из-за монотонности счётчиков в пределах прогона. Аномалии — два латченных флага: **`divergent`** (внутри **одного** набора имён больше одной различающейся пары `(input,output)` — конфликт сопоставимых величин) и **`non_monotonic`** (per-step выше кумулятива между наборами — нарушен порядок). ⚠️ Расхождение **между** наборами флагом не является: на шаге >1 per-step есть дельта, а `cumulative_*` — итог. Частично прочитанный носитель помечается `partial` (и берётся, только если полных совпадений нет); нецелочисленное значение распознаванием не считается; при `owed == 0` на блоке, не являющемся доказуемо usage-free, пишется **WARNING**. Это делает промах **наблюдаемым постфактум**, но не предотвращает его: неизвестный носитель по-прежнему тарифицируется в ноль. **Артефакт исполнителем не добывается** — нужен прогон против реального образа, поэтому вопрос маршрутизирован архитектору и закрывается только предъявлением сырой капчи (по образцу `tests/fixtures/hermes_prod_run_adr065.sse`). До закрытия: `production_ready: true` для агентного биллинга при флаге OFF не выставлять. Cross-ref: [05-events.md](modules/agent-proxy/05-events.md), [09-testing.md](modules/agent-proxy/09-testing.md), [06-testing-strategy.md](06-testing-strategy.md), [Q-067-11](99-open-questions.md). | Нет (но блокирует приёмку биллинга при флаге OFF) |
| Q-067-11 | **Формы `tool.started` / `tool.completed` / `approval.request` не подтверждены капчей** — прогон, с которого снята `tests/fixtures/hermes_prod_run_adr065.sse`, шёл без вызова инструментов и без запроса подтверждения (баланс исчерпан раньше). Ключи-носители имени инструмента и превью — **допущение**. | Open — владелец добычи: **qa** (прогон с гарантированным вызовом инструмента и с approval-сценарием) | **Почему отдельным номером от [Q-067-10](99-open-questions.md), а не общим:** разная цена и разный закрывающий артефакт. Здесь путь — **снапшот/UI**, не деньги: промах формы даёт `last_tool` замороженным и `pending_approval` незаполненным, то есть `/state` не отдаст `waiting_approval` и клиент не покажет запрос подтверждения ([ADR-066 §4](adr/ADR-066-agent-run-state-snapshot.md)) — деградация UX, наблюдаемая пользователем, без финансовых последствий. Закрывается **другим** прогоном (с инструментами), чем Q-067-10 (со штатным завершением), поэтому объединение позволило бы дешёвому артефакту закрыть дорогой вопрос. **Смягчение в коде (реализовано):** извлекатели имени инструмента и превью shape-толерантны — пробуют `tool`/`tool_name`/`name`, каждый в форме строки либо обёртки `{name/tool/tool_name}`; превью — `preview`/`description`/`summary`, строка либо `{text}`. Неизвестная форма деградирует в `None`, а не рвёт relay. **Дополнительно не подтверждено:** несут ли эти события `timestamp` ([Q-067-7](99-open-questions.md), H5). | Нет |

## Блокеры (для orchestrator)
- ~~**Q-015-1 (покупка токенов × policy)**~~ — **Closed (2026-06-02, вариант б):** покупка токенов требует активной подписки (`403 subscription_required` до grant), [ADR-002](adr/ADR-002-access-policy-state-machine.md) без изменений. Требует backend-доработки: policy-guard перед `WalletService.grant` в token-purchase. См. [ADR-015 §Доступность](adr/ADR-015-consumable-token-iap.md).
- **Q-016-2 (web search)** — блокирует **только** фичу веб-поиска: нет выбора провайдера → нет контракта server-side tool. Остальное расширение не блокирует.
- **Q-016-1 (Actions/styles)** — помечен для пользователя; backend реализуем на дефолте (клиентские пресеты), ответ влияет на детали (нужен ли endpoint пресетов).
- ~~**Q-017-1 / Q-017-2 (домен + certresolver)**~~ — **Closed (2026-06-02, значения пользователя/владельца):** `SERVICE_DOMAIN=broadnova.shop`, `TRAEFIK_CERTRESOLVER=le` (default на `websecure`). Предзапусковый операционный пункт — A-запись `broadnova.shop` → `87.239.135.154` (для ACME) — остаётся в [prod-checklist](07-deployment.md#prod-readiness-checklist-must-configure-before-launch).
- ~~**Q-005-1 (JWT issuer)**~~ — **Closed (2026-06-02):** встроенный issuer в backend ([ADR-018](adr/ADR-018-embedded-auth-issuer.md), модуль [auth](modules/auth/README.md)), device-based. Prod-блокер «реальный issuer» снят; остаётся операционный пункт «сконфигурировать приватный ключ подписи» в [prod-checklist](07-deployment.md#prod-readiness-checklist-must-configure-before-launch). Backend-доработка: модуль auth + миграция 0005 ([modules/auth/07-implementation-phases.md](modules/auth/07-implementation-phases.md)).
- ~~**Q-024-1 (сопутствующий текст при `tool_call`)**~~ — **Closed (2026-06-10, вариант A):** при `status=tool_call` `ChatResponse` опционально возвращает `assistantMessage` (текст того же assistant-шага); backend перестаёт отбрасывать текст (`orchestrator.py:661`). Аддитивно/обратносовместимо. Не блокировал [ADR-024](adr/ADR-024-history-payload-domain-normalization.md) (нормализация истории внедрена независимо). Требует backend-доработки: заполнять `assistantMessage` из `text`-блоков того же assistant-шага при `tool_call`.
- ~~**Q-018-2 (апгрейд идентичности / Apple Sign-In)**~~ — **Closed (2026-06-19):** Sign in with Apple ([ADR-043](adr/ADR-043-sign-in-with-apple.md), `POST /v1/auth/apple`, таблица `auth_identities`, миграция `0012`). Кросс-девайс аккаунт. Требует backend-доработки (config `APPLE_*` + `AppleIdentityVerifier` + `AuthService.sign_in_with_apple` + миграция `0012` + роутер/схема). Новые Q-043-1/Q-043-2 неблокирующие.
- Прочих активных блокеров нет. Q-004-1 и Q-006-1 закрыты [ADR-006](adr/ADR-006-credit-billing-and-subscription-grant.md).
- Admin (Фича A) и website-builder (Фича B) спроектированы с разумными дефолтами; открытые вопросы Q-009-* / Q-010-* неблокирующие.
