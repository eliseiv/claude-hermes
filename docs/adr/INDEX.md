# ADR Index

Реестр архитектурных решений. Статусы: Proposed / Accepted / Superseded.

| ADR | Заголовок | Статус | Дата |
|---|---|---|---|
| [ADR-001](ADR-001-stack-choice.md) | Выбор стека: Python + FastAPI + PostgreSQL, модульный монолит | Accepted | 2026-05-21 |
| [ADR-002](ADR-002-access-policy-state-machine.md) | Политика доступа как state machine (trial → subscription → credits/byok) | Accepted | 2026-05-21 |
| [ADR-003](ADR-003-byok-envelope-encryption.md) | BYOK: envelope encryption (AES-256-GCM + KMS) | Accepted (ревизия 2026-06-02) | 2026-05-21 |
| [ADR-004](ADR-004-blocked-http-200.md) | HTTP 200 для бизнес-blocked + стандартизированный blockReason enum | Accepted | 2026-05-21 |
| [ADR-005](ADR-005-idempotency-ledger.md) | Атомарность и идемпотентность ledger и tool-result | Accepted | 2026-05-21 |
| [ADR-006](ADR-006-credit-billing-and-subscription-grant.md) | Биллинг: 1 кредит = 1 сообщение; фикс. пакет кредитов на период подписки | Accepted | 2026-05-21 |
| [ADR-007](ADR-007-lazy-user-provisioning.md) | Ленивый провижининг users из доверенного JWT subject (upsert в gateway) | Accepted | 2026-05-25 |
| [ADR-008](ADR-008-provider-tool-use-id.md) | Раздельное хранение provider tool_use.id (`toolu_...`) для согласованности continuation | Accepted | 2026-05-25 |
| [ADR-009](ADR-009-admin-token-auth.md) | Admin-авторизация: изолированный admin-токен (`X-Admin-Token`, статический secret), ротация, невозможность эскалации | Accepted | 2026-06-01 |
| [ADR-010](ADR-010-backend-hosted-preview.md) | Backend-hosted preview сайтов: signed URL (HMAC+TTL) + threat model отдачи пользовательского HTML/JS | Accepted | 2026-06-01 |
| [ADR-011](ADR-011-server-side-tools.md) | Server-side tools (`site.*`): backend исполняет в tool-loop, не отдаёт клиенту | Accepted | 2026-06-01 |
| [ADR-012](ADR-012-assistant-mode-vs-billing-mode.md) | Разведение терминологии: `assistant_mode` (chat/code, тип ассистента) vs `billing_mode` (credits/byok, способ оплаты) | Accepted | 2026-06-02 |
| [ADR-013](ADR-013-workspace-projects-vs-website-builder.md) | Workspace-проекты (рабочие пространства чатов) — отдельный модуль, не website-builder `projects` | Accepted | 2026-06-02 |
| [ADR-014](ADR-014-multimodal-attachments.md) | Мультимодальный ввод: двухшаговые вложения (upload `/v1/attachments` → ссылка в `/chat/run`) | **Superseded (транспорт) → [ADR-020](ADR-020-inline-base64-attachments-mvp.md)** | 2026-06-02 |
| [ADR-015](ADR-015-consumable-token-iap.md) | Покупка токенов: consumable StoreKit IAP → идемпотентный grant кредитов (отдельно от подписки) | Accepted | 2026-06-02 |
| [ADR-016](ADR-016-extended-byok-statuses.md) | Расширенные BYOK-статусы (`validating`/`offline`/`expired`) + активная модель в ответе, обратная совместимость | Accepted | 2026-06-02 |
| [ADR-017](ADR-017-shared-server-traefik-deploy.md) | Deploy-топология: общий сервер за внешним Traefik + GitHub Actions SSH (ревизует TD-005 VPS+Caddy); расширен 2026-06-10: мульти-инстанс / клонирование (`COMPOSE_PROJECT_NAME`). **Ревизия 2026-07-15: для `claude-hermes` применяется ТОЛЬКО выделенный `.156` ([ADR-057](ADR-057-dedicated-server-self-hosted-traefik-deploy.md)); broadnova `.154` INSTANCES-loop удалён из CI (был fork-артефакт claude-ios)** | Accepted (ревизия 2026-07-15) | 2026-06-02 |
| [ADR-018](ADR-018-embedded-auth-issuer.md) | Встроенный auth-issuer в backend (device-based identity, RS256, refresh-rotation) — закрывает Q-005-1 | Accepted | 2026-06-02 |
| [ADR-019](ADR-019-tools-catalog-endpoint.md) | Каталог инструментов `GET /v1/tools` (JWT-protected, источник — chat/tools.py) | Accepted | 2026-06-02 |
| [ADR-020](ADR-020-inline-base64-attachments-mvp.md) | Мультимодальный ввод: inline base64-вложения в `/chat/run` (MVP); заменяет транспорт ADR-014 | Accepted | 2026-06-03 |
| [ADR-021](ADR-021-deterministic-step-order-and-block-normalization.md) | Детерминированный порядок шагов сессии (монотонный `chat_steps.seq`) + нормализация content-блоков перед персистом (BUG-5) | Accepted | 2026-06-04 |
| [ADR-022](ADR-022-optional-project-and-tool-gating.md) | Опциональный `projectId` в `/v1/chat/run` + гейтинг `site.*` по наличию проекта (website-builder — опциональная фича; «чистый чат» без проекта) | Accepted | 2026-06-04 |
| [ADR-023](ADR-023-sync-ids-in-chat-response.md) | Идентификаторы синхронизации в `ChatResponse` (`messageStepId` + `stepId`, nullable при blocked) — склейка ответа генерации с историей чата | Accepted | 2026-06-05 |
| [ADR-024](ADR-024-history-payload-domain-normalization.md) | Нормализация content-блоков истории `GET /v1/chats/{id}` `steps[].payload` к доменному виду (dot-имя + domain id) при отдаче — хранение/реплей не меняются | Accepted | 2026-06-10 |
| [ADR-025](ADR-025-parallel-tool-calls-and-max-tokens-truncation.md) | Параллельные client-side tool-вызовы (`toolCalls[]` в `ChatResponse` + батч `/chat/tool-result`, барьер хода) + обработка обрезки по `max_tokens` (`blockReason=max_tokens`, дефолт `ANTHROPIC_MAX_TOKENS=16000`) | Accepted | 2026-06-10 |
| [ADR-026](ADR-026-global-server-side-tools-and-time-now.md) | Project-independent (global) server-side tools (`GLOBAL_SERVER_SIDE_TOOLS`, отдельно от project-scoped `site.*`) + инструмент `time.now` (UTC всегда + локальное по опц. IANA `tz`); работает без проекта, не мутирует, без новых списаний; статичная generic-инструкция в промт (кэш не ломается) | Accepted | 2026-06-10 |
| [ADR-027](ADR-027-calendar-read-contract-alignment.md) | Унификация контракта `calendar.read` с `calendar.create_events`: переименование args `startDate`/`endDate` → `start`/`end` + переход на ISO8601 datetime (naive local), end-exclusive «весь день»; **breaking change iOS-контракта** (требует обновления клиента); формат закрепляется в `TOOL_DESCRIPTIONS`, серверная datetime-валидация не вводится (симметрично read/create) | Accepted | 2026-06-11 |
| [ADR-028](ADR-028-projectid-in-chat-list-and-server-tools-in-chat-response.md) | Два **аддитивных** (не breaking) исправления контракта по репортам iOS: (1) `projectId` (свободная строка = `chat_sessions.project_id`, [ADR-022](ADR-022-optional-project-and-tool-gating.md), `null`=«чистый чат») в элементе `GET /v1/chats` рядом с неизменным `workspaceProjectId`; (2) массив `serverTools[]` выполненных server-side инструментов (`site.*`/`time.now`) в `ChatResponse` `/chat/run` + `/chat/tool-result` — `{toolName, status: completed\|errored, summary?}`, компактный `summary` (≤120, без raw/путей/URL/токенов), семантика «за один вызов», присутствует во всех не-policy статусах; биллинг неизменен ([ADR-006](ADR-006-credit-billing-and-subscription-grant.md)) | Accepted | 2026-06-12 |
| [ADR-029](ADR-029-adapty-subscription-webhook.md) | Adapty subscription webhook (`POST /v1/billing/adapty/webhook`) — **основной путь биллинга по подпискам**: статический bearer-секрет (constant-time, без HMAC-подписи payload — ограничение Adapty), сырое тело без Pydantic, **2xx `ignored` на любой кривой/неизвестный payload** после авторизации (анти-абуз бесконечных ретраев Adapty; 5xx только при реальном сбое), 4 события (started/renewed→active+грант кредитов по тиру; cancelled/expired→expired), идемпотентность через новую таблицу `adapty_webhook_events` (UNIQUE `event_id`) + ledger `idempotency_key` в одной транзакции, audit `adapty_subscription`. `/v1/subscription/sync` и `/v1/tokens/purchase` сохранены; источник истины подписок = Adapty; клиент использует ОДИН путь (риск двойного начисления). Модуль [billing-adapty](../modules/billing-adapty/README.md) | Accepted | 2026-06-12 |
| [ADR-030](ADR-030-toolcallid-in-server-tools.md) | **Аддитивно** (не breaking) к [ADR-028](ADR-028-projectid-in-chat-list-and-server-tools-in-chat-response.md): поле `toolCallId` (доменный uuid4 = `tool_calls.id`) в элементе `serverTools[]` ответа `/chat/run` + `/chat/tool-result` — корреляция записи `serverTools[]` с tool-шагом истории `GET /v1/chats/{id}` (по `payload.toolCallId`); обязательное, первым полем, симметрично client-side `toolCalls[].id` ([ADR-008](ADR-008-provider-tool-use-id.md)/[ADR-024](ADR-024-history-payload-domain-normalization.md)); биллинг/каталог/число инструментов неизменны | Accepted | 2026-06-15 |
| [ADR-031](ADR-031-absolute-preview-url.md) | `site.preview` возвращает **абсолютный** URL `https://<SERVICE_DOMAIN>/v1/preview/{projectId}/{token}/{path}` (вместо относительного пути) — фикс галлюцинации хоста моделью; `SERVICE_DOMAIN` теперь читается app-config (`src/app/config.py`, PUBLIC, с нормализацией strip-протокола/хвостового слеша), на проде уже задан в `.env` обоих инстансов; пустой → fallback на относительный путь (dev); описание `site.preview` дополнено «использовать URL как есть, не менять хост»; модель угроз неизменна ([ADR-010](ADR-010-backend-hosted-preview.md)), каталог 14 инструментов без изменений | Accepted | 2026-06-16 |
| [ADR-032](ADR-032-notifications-enabled-default-false.md) | Контрактный дефолт `notificationsEnabled` → `false` (privacy-by-default; iOS-flow «системное разрешение сначала»): меняется ТОЛЬКО дефолт для новых/без-строки пользователей (сервисный lazy-default `_defaults()` + `server_default` колонки), существующие строки `user_preferences` НЕ трогаются (миграция без backfill, без `UPDATE`); Pydantic-схема не меняется; усиливает private-by-default ([TD-011](../100-known-tech-debt.md)) | Accepted | 2026-06-16 |
| [ADR-033](ADR-033-llm-provider-abstraction.md) | Провайдер-абстракция LLM (Anthropic \| OpenAI): нейтральный интерфейс `LLMClient`, нормализованный `stop_reason` ∈ {tool_use, max_tokens, end_turn}, factory `get_llm_client()` по `LLM_PROVIDER` (дефолт `anthropic`); один провайдер на инстанс (БД хранит wire-формат своего провайдера); OpenAI через Chat Completions (function-calling + vision, non-streaming); провайдер-специфичная (де)сериализация — внутри клиента, orchestrator/персист провайдер-агностичны; underscore-map имён tools переиспользуется (точки запрещены у обоих); attachments OpenAI = image_url data-URI + текст, **PDF → 422**; BYOK/config/observability per-provider; 3-й инстанс `LLM_PROVIDER=openai` | Accepted | 2026-06-16 |
| [ADR-034](ADR-034-user-model-selection.md) | Выбор модели пользователем: allowlist per-provider (`ANTHROPIC_MODELS`/`OPENAI_MODELS`, JSON id→displayName, фолбэк на единственную дефолтную при пустом) + `GET /v1/models` (JWT, `[{id, displayName, default}]` активного провайдера) + **session-fixed** `ChatRunRequest.model` (колонка `chat_sessions.model`, миграция 0010, nullable; resume → из сессии, поле игнорируется; неизвестная модель → `422 unsupported_model`); `LLMClient.create_message(..., model=None)` (None → дефолт клиента); биллинг неизменен (1 кредит, [ADR-006](ADR-006-credit-billing-and-subscription-grant.md)); обратная совместимость полная (без env/без поля → текущее поведение) | Accepted | 2026-06-17 |
| [ADR-035](ADR-035-prompt-presets-endpoint.md) | Пресеты промтов: статический реестр в коде (`src/app/chat/presets.py`, single source of truth, по образцу `tool_catalog`) + `GET /v1/presets` (JWT, `{presets:[{id,title,icon,prompt}]}`, провайдер/инстанс-агностично, одинаково на 3 инстансах); `id` — стабильный slug, `icon` — имя SF Symbol, `prompt` — текст в композер; 7 дефолтных пресетов (Plan Week/Meeting Notes/Tasks from Photo/Design Brief/Daily Review/Summarize Text/Project Structure); без БД/env/миграции/биллинга, аддитивно; config-JSON env → отложено ([TD-026](../100-known-tech-debt.md)), БД-CRUD → [Q-035-1](../99-open-questions.md), i18n → [Q-035-2](../99-open-questions.md) | Accepted | 2026-06-17 |
| [ADR-036](ADR-036-workspaces-implementation.md) | **Реализация Workspaces (рабочие пространства)** — расширяет [ADR-013](ADR-013-workspace-projects-vs-website-builder.md), частично супершедит прежнее проектирование модуля. Поставка 3, 2 под-фазы. **3A ядро:** таблица `workspace_projects` (13) + `chat_sessions.workspace_project_id` (nullable FK, ON DELETE SET NULL), миграция `0011`; CRUD `/v1/workspaces` (path = workspaces, НЕ projects — разведение ADR-013; курсорная пагинация как chats), изоляция по `sub` (`404`); `ChatRunRequest.workspaceProjectId` session-fixed (валидация принадлежности → `404`), orchestrator инъектирует `workspace.instructions` в system-prompt ПОСЛЕ base assistant_mode prompt; `ChatListItemSchema.workspaceProjectId` теперь реальный + фильтр `GET /v1/chats?workspaceProjectId=`; удаление → файлы CASCADE, чаты SET NULL (orphan). **3B файлы-знания:** **собственная таблица `workspace_files` (14) BYTEA** (образец `site_files`, [TD-027](../100-known-tech-debt.md)), **НЕ через `attachments`** (разблокирует фичу, не ждёт [TD-015](../100-known-tech-debt.md)); inline base64 upload (reuse валидаций [ADR-020](ADR-020-inline-base64-attachments-mvp.md)); извлечение текста (pypdf/decode) → `extracted_text`; инъекция в чаты workspace: document/text → `extracted_text` (работает на обоих провайдерах, PDF→422 [TD-023](../100-known-tech-debt.md) НЕ применяется), image → vision; лимит `WORKSPACE_CONTEXT_MAX_CHARS` (усечение, RAG отложено [Q-013-1](../99-open-questions.md)). Биллинг неизменен ([ADR-006](ADR-006-credit-billing-and-subscription-grant.md)): CRUD/файлы бесплатно, генерация 1 кредит. Модуль [workspaces](../modules/workspaces/README.md) | Accepted | 2026-06-17 |
| [ADR-037](ADR-037-chatrunrequest-context-allowlist-injection.md) | **`ChatRunRequest.context` функционален** — раньше только size-валидировался и игнорировался. Теперь **per-message** (НЕ session-fixed, **без миграции БД**): фиксированный **allowlist** ключей (`codeLanguage` str≤40, `responseStyle` enum `concise\|balanced\|detailed`, `verbosity` enum `low\|medium\|high`, `tone` str≤40, `locale` str≤35 BCP-47-подобный) с per-key валидацией; неизвестные ключи и невалидные значения — **игнорируются** (lenient, forward-compat), size-guard `size_limit_context` (≤64KB→422) сохранён. Helper `_render_context_block()` → детерминированный компактный текст-блок (фикс. порядок, экранирование), инъекция в **user-сообщение turn0** (лидирует, `\n\n` + `message`), **НЕ в system** (prompt-кэш не ломается, нет privilege-escalation). Персист в `chat_steps.payload` (корректный replay); на continuation/tool-result не повторяется. Провайдер-агностично ([ADR-033](ADR-033-llm-provider-abstraction.md)), биллинг неизменен ([ADR-006](ADR-006-credit-billing-and-subscription-grant.md)), обратная совместимость полная. Расширение allowlist → [Q-037-1](../99-open-questions.md); связь с `code_defaults` → [TD-028](../100-known-tech-debt.md) | Accepted | 2026-06-17 |
| [ADR-038](ADR-038-move-chat-to-workspace.md) | **Перенос существующего чата в воркспейс** — расширяет [ADR-036](ADR-036-workspaces-implementation.md): `chat_sessions.workspace_project_id` становится **изменяемым** через единый `PATCH /v1/chats/{id}` с полем `workspaceProjectId: uuid\|null` (перенести/сменить/убрать=null; различение absent vs null по `model_fields_set`; **аддитивно**, без миграции — колонка из `0011`). Валидация: чат принадлежит user→`404`, целевой workspace принадлежит user→**`404 workspace_not_found`** (консистентно с `/chat/run`), идемпотентно. **КЛЮЧЕВОЕ изменение orchestrator:** инъекция `instructions` **развязывается от `ctx.is_new`** → подаётся в `system` на **КАЖДОМ** ходе сессии с workspace (turn 0/resume/continuation), чтобы перенесённый чат получал инструкции проекта дальше (через `instructions_for_session`); **файлы-знания остаются turn-0-only** (вариант a — НЕ переинъектируются ретроспективно: стоимость токенов/контекст-окно/prompt-cache, симметрия с inline-attachments [ADR-020](ADR-020-inline-base64-attachments-mvp.md)); ретроактивная подача файлов → [Q-038-1](../99-open-questions.md). `/chat/run` `workspaceProjectId` остаётся **session-fixed** (resume игнорирует), управление вынесено в `PATCH`. Биллинг неизменен ([ADR-006](ADR-006-credit-billing-and-subscription-grant.md)), аудит-событие не вводится | Accepted | 2026-06-18 |
| [ADR-039](ADR-039-optional-message-with-attachments.md) | **Опциональный `message` в `/v1/chat/run` при наличии вложений** (image-only / file-only ход) — снять `min_length=1` (`message: str = ""`), валидатор «message-после-strip непуст ИЛИ ≥1 attachment запроса» (иначе `422 "message or at least one attachment is required"`, size-лимит message сохранён); orchestrator собирает turn-0 user-content **без** text-блока при пустом тексте (только attachment-блоки) — пустой text-блок не отправляется НИ Anthropic, НИ OpenAI ([ADR-033](ADR-033-llm-provider-abstraction.md), провайдер-агностично); склейка с context-блоком ([ADR-037](ADR-037-chatrunrequest-context-allowlist-injection.md)): пустой message+блок → текст=блок (без висячего `\n\n`); workspace-файлы ([ADR-036](ADR-036-workspaces-implementation.md)) требование «≥1 attachment» НЕ удовлетворяют (про `attachments` запроса); биллинг неизменен (1 кредит, [ADR-006](ADR-006-credit-billing-and-subscription-grant.md)), без миграции, аддитивно (обратная совместимость полная) | Accepted | 2026-06-18 |
| [ADR-040](ADR-040-edit-message-and-regenerate.md) | **Редактирование отправленного сообщения** (расширяет [ADR-021](ADR-021-deterministic-step-order-and-block-normalization.md) `seq` / [ADR-023](ADR-023-sync-ids-in-chat-response.md) `messageStepId`) — поле `ChatRunRequest.editMessageStepId: uuid\|null` в существующем `POST /v1/chat/run` (один атомарный вызов): усечь историю от хода `editMessageStepId` (его user-шаг по мин. `seq` + всё с `seq >= anchor`) и сгенерировать новый ход. **Требует `sessionId`** (resume): `editMessageStepId` без `sessionId` → `422`; чужая/несуществующая/истёкшая сессия → `404`; нет user-шага с этим `message_step_id` → `404 message_not_found` (anchor строго по `role='user'`, edit assistant/tool-шага → 404). Усечение в транзакции запроса до `add_step(user)`: удалить `chat_steps` (`seq >= anchor`) + **явно** `tool_calls` усечённых ходов по `message_step_id` (FK `tool_calls` на `session_id`, НЕ на шаг → каскад не срабатывает, иначе осиротеют). Биллинг: регенерация = новый `message_step_id` → **новый дебит 1 кредита** ([ADR-006](ADR-006-credit-billing-and-subscription-grant.md)), **возврата за старый ход НЕТ** (no-refund-on-edit). Edge: edit первого сообщения → сессия пуста, но `is_new=False` → workspace-файлы НЕ реинъектируются (вариант a, [ADR-038](ADR-038-move-chat-to-workspace.md)), instructions — как обычно; открытый tool-loop усекается (без осиротевших `tool_calls`/барьеров). Изоляция: усечение только в проверенной на владельца сессии. Без миграции, обратная совместимость полная (без поля `/chat/run` неизменен) | Accepted | 2026-06-19 |
| [ADR-041](ADR-041-openai-native-pdf-attachment.md) | **Нативная поддержка PDF-вложений на OpenAI-инстансах (снятие 422)** — implementation-ADR, расширяет [ADR-020](ADR-020-inline-base64-attachments-mvp.md) (класс `document`/PDF) и [ADR-033 §5](ADR-033-llm-provider-abstraction.md) (провайдер-маппинг), **закрывает [TD-023](../100-known-tech-debt.md)**. В openai-ветке маппинга (`src/app/chat/attachments.py::_openai_content_block`, `document`) `application/pdf` больше **НЕ** `422`: **основной путь** — OpenAI Chat Completions content-часть `file` (`{"type":"file","file":{"filename","file_data":"data:application/pdf;base64,..."}}`, backend сверяет точный wire-shape с установленным `openai` SDK 1.109.1 реальным вызовом); **гарантированный фолбэк** (если SDK/endpoint не принимает `file`-часть) — серверное извлечение текста PDF через `pypdf` (переиспользование паттерна `workspaces/text_extract.py`) → text-блок. Anthropic-ветка (нативный `document`-блок) не трогается; валидация/лимиты (magic-bytes, page-guard ≤100, doc ≤8 MB — в пределах OpenAI 100стр/32MB) не меняются; сырой base64 не персистится (плейсхолдеры, [ADR-020 §3](ADR-020-inline-base64-attachments-mvp.md)); `filename` — из `AttachmentIn.filename` (дефолт `file`); биллинг неизменен (1 кредит, [ADR-006](ADR-006-credit-billing-and-subscription-grant.md)); без миграции | Accepted | 2026-06-19 |
| [ADR-042](ADR-042-hide-context-block-from-user-facing-history.md) | **Скрытие conversation-settings блока ([ADR-037](ADR-037-chatrunrequest-context-allowlist-injection.md)) из user-facing вывода** — implementation-ADR (read-time strip), **ревизует ADR-037 в части отображения**. Репорт iOS: служебный блок `[Conversation settings for this message: …]` персистится внутри текста user-сообщения (ADR-037 §4) и потому виден в истории `GET /v1/chats/{id}` (`steps[].payload.content[0].text`) и в превью `GET /v1/chats`. Решение (вариант B): срезать ведущий блок **только при отдаче** через единый helper `strip_context_block` (источник истины формата), применяемый в `_normalize_payload` (история, на копии — паттерн [ADR-024](ADR-024-history-payload-domain-normalization.md)) и `_text_summary` (превью); якорь `^\[Conversation settings for this message: [^\]]*\]\n\n` (только начало, точный сервер-формат) + edge image-only-с-context (блок == весь текст → пустой текст + attachment-плейсхолдеры). **Только user-шаги, только лидирующий text-блок, нет блока → no-op.** Хранимое `chat_steps.payload` и реплей `_build_messages` **НЕ меняются** (модель получает блок как прежде, генерация/контекст неизменны), без миграции, провайдер-агностично, биллинг неизменен ([ADR-006](ADR-006-credit-billing-and-subscription-grant.md)) | Accepted | 2026-06-19 |
| [ADR-043](ADR-043-sign-in-with-apple.md) | **Sign in with Apple** (`POST /v1/auth/apple`) — implementation-ADR, расширяет [ADR-018](ADR-018-embedded-auth-issuer.md), **закрывает [Q-018-2](../99-open-questions.md)**. iOS шлёт Apple **identity token** (OIDC JWT, RS256, нативный flow: `iss=https://appleid.apple.com`, `aud`=bundle id); backend верифицирует и выдаёт **НАШУ** пару токенов (`TokenResponse`, как `register`). Верификатор `src/app/auth/apple.py::AppleIdentityVerifier` — alg-ветвление: HS256→test-mode (`APPLE_TEST_MODE`+`APPLE_TEST_SECRET`, иначе `401` fail-closed) / RS256→`PyJWKClient(APPLE_JWKS_URL)`→`jwt.decode(issuer=APPLE_OIDC_ISSUER, audience=bundle, require sub/iss/aud/exp)`; nonce опционален (валидируется `sha256(nonce)==claim` при наличии обеих сторон); токен/nonce НЕ логируются. Config `APPLE_*` (`APPLE_OIDC_ISSUER`/`APPLE_JWKS_URL`/`APPLE_AUDIENCE` (фолбэк `APPSTORE_BUNDLE_ID`)/`APPLE_TEST_MODE`/`APPLE_TEST_SECRET`; кэш JWKS = `jwks_cache_ttl_seconds`); аудитория не задана → `503`. Новая таблица `auth_identities(provider,subject)` UNIQUE + миграция `0012` (down_revision=`0011`, single head). Связывание: `apple_sub` известен → тот же `userId` (кросс-девайс); неизвестен → привязать к device-аккаунту, если у него нет Apple-идентичности (сохранить кредиты/историю), иначе новый пользователь; конфликт apple_sub-user≠device-user → берём apple_sub-user, устройство upsert на него, авто-merge данных НЕТ ([Q-043-2](../99-open-questions.md)). Невалид/iss/aud/подпись/exp/nonce → `401`; под `enforce_auth_limits`; биллинг неизменен; провайдер-агностично | Accepted | 2026-06-19 |
| [ADR-044](ADR-044-client-api-key-auth.md) | **Авторизация клиентским API-KEY** (`X-API-Key`) + идентичность через `X-User-Id` (UUID) — клиентский контур `/v1/*` (Hermes-интеграция). `verify_client_api_key()` (constant-time vs `CLIENT_API_KEY`, ротация `CLIENT_API_KEY_PREV`); `get_current_user` переписан: identity из `X-User-Id` (доверяется, т.к. клиентский ключ доверенный), lazy provisioning ([ADR-007](ADR-007-lazy-user-provisioning.md)) **сохраняется** (меняется лишь источник `userId`). `require_owner` → no-op (субъект = `X-User-Id`). **JWT/Apple ([ADR-018](ADR-018-embedded-auth-issuer.md)/[ADR-043](ADR-043-sign-in-with-apple.md)) «дремлют»** — НЕ удаляются (БД-схема `auth_*`/тесты не ломаются), роутеры перестают использоваться клиентом. Swagger: `bearerAuth`→две `APIKeyHeader` (`clientApiKey`+`userId`); `adminToken` ([ADR-009](ADR-009-admin-token-auth.md)) неизменна. Модель угроз: единый ключ — высокопривилегированный секрет (компрометация = все `userId`); усиление — [Q-044-1](../99-open-questions.md) | Accepted | 2026-06-23 |
| [ADR-045](ADR-045-hermes-as-agent-proxy.md) | **Hermes-as-agent: контур `/v1/agent/*`** — прокси к персональному Hermes-инстансу (`POST /v1/runs`, SSE `/events`, `/approval`, `/stop`). Hermes — полноценный агент (свой tool-loop/память/навыки), **НЕ заводится в `LLMClient.create_message`** ([ADR-033](ADR-033-llm-provider-abstraction.md)). `/v1/chat/*` остаётся «простым чатом». Поток `run`: auth→policy-gate ([ADR-047](ADR-047-usage-based-billing-for-agent.md))→`ensure_running(userId)` ([ADR-046](ADR-046-per-user-hermes-runtime.md))→прокси `POST /v1/runs` (`Authorization: Bearer <API_SERVER_KEY>`)→`202 {runId}`. SSE-ретрансляция; на `run.completed{usage}`→`WalletService.consume(idempotency_key=runId)`; `run.failed`→без debit. Маппинг тела `message→input`/`sessionId→session_id`/`model→model`. Адресация — DNS контейнера в docker-сети. `httpx.AsyncClient` | Accepted | 2026-06-23 |
| [ADR-046](ADR-046-per-user-hermes-runtime.md) | **Per-user Hermes runtime** — модуль `src/app/hermes_runtime/` (`HermesInstanceManager`, `docker_backend`, `registry`): Docker-инстанс Hermes на пользователя (том `HERMES_HOME`, env `API_SERVER_*`/`LLM_*`, `gateway run`, порт НЕ публикуется, DNS-адресация `hermes-user-<id>:8642`). Жизненный цикл `ensure_running`/`provision`/`stop_idle`/`deprovision`/`health`; гибернация (стоп простаивающих по `HERMES_IDLE_TIMEOUT_SECONDS`, пробуждение по запросу) + фоновый reaper в `lifespan`. Расширяемый `RuntimeBackend` (задел Modal/Daytona; MVP — docker-py). Новая таблица `hermes_instances` (`user_id` PK, `container_id`, `endpoint`, `api_key_enc`, `status`, `port?`, `last_active_at`, `created_at`), миграция `0013`. `API_SERVER_KEY` шифруется envelope-схемой через `byok.kms` ([ADR-003](ADR-003-byok-envelope-encryption.md)). Toolset `[web,file,vision,skills,todo]` (без terminal/browser/code_execution/computer_use) через `config.yaml` тома. **Ревизия 2026-07-15 → [ADR-065](ADR-065-patched-hermes-image-ghcr.md): источник `HERMES_IMAGE` = наш патченый `ghcr.io/eliseiv/hermes-agent@sha256:<digest>` (не upstream `nousresearch/*`), digest-pin.** | Accepted (ревизия 2026-07-15) | 2026-06-23 |
| [ADR-047](ADR-047-usage-based-billing-for-agent.md) | **Биллинг агентного пути по реальному usage** (токены), idempotency по `runId` — **расширяет / частично-суперсидит [ADR-006](ADR-006-credit-billing-and-subscription-grant.md) ДЛЯ `/v1/agent/*`** (`/v1/chat/*` остаётся «1 кредит = 1 сообщение»). Формула `amount = ceil(input/1000*CREDITS_PER_1K_INPUT + output/1000*CREDITS_PER_1K_OUTPUT)`, мин. 1 при ненулевом usage (кредиты целые, округление вверх). Policy-gate ([ADR-002](ADR-002-access-policy-state-machine.md): BR-2/BR-3/BR-5) **до** прогона; blocked = HTTP 200 ([ADR-004](ADR-004-blocked-http-200.md)). `WalletService.consume(idempotency_key=runId, meta={usage,model,source:agent_run})` на `run.completed`; `run.failed`→без debit (нет usage). Idempotency ([ADR-005](ADR-005-idempotency-ledger.md)) и grant при подписке ([ADR-006 §2](ADR-006-credit-billing-and-subscription-grant.md)) — общие. Дефолты коэффициентов/округление — [Q-047-1](../99-open-questions.md); pre-debit hold/reconcile — [Q-047-2](../99-open-questions.md); BYOK агента — [Q-047-3](../99-open-questions.md). **§6 (data-integrity):** при `InsufficientCredits` на агентном `run.completed` `consume` самодостаточно-атомарен (SAVEPOINT-откат INSERT+UPDATE — **нет orphan debit-строки**, не зависит от внешнего ROLLBACK проглоченного исключения на SSE-пути); несписанная дельта — audit `billing_debit_insufficient` (не ledger-строка); вариант «orphan как ledger-флаг» отвергнут (ломает сверку balance↔ledger). Реконсиляция — [TD-029](../100-known-tech-debt.md) | Accepted | 2026-06-23 |
| [ADR-048](ADR-048-admin-credits-and-subscription-grant.md) | **Два admin-эндпоинта** под `require_admin`/`X-Admin-Token` ([ADR-009](ADR-009-admin-token-auth.md), неизменна). (1) `POST /v1/admin/credits/grant` — реюз/переименование `/v1/admin/wallet/grant` (`WalletService.grant`, идемпотентно, `404 user_not_found` на несуществующем). (2) `POST /v1/admin/subscription/grant` — **новый** `SubscriptionService.admin_grant(user_id, plan, expires_at)`: upsert `subscriptions`→active + опц. грант `SUBSCRIPTION_CREDITS_PER_PERIOD` ([ADR-006](ADR-006-credit-billing-and-subscription-grant.md), идемпотентно), audit `admin_subscription_grant`, `404` на несуществующем ([Q-009-2](../99-open-questions.md)). После выдачи подписки `/v1/agent/run` проходит policy ([ADR-002](ADR-002-access-policy-state-machine.md)). Swagger `adminToken` неизменна | Accepted | 2026-06-23 |
| [ADR-049](ADR-049-redaction-usage-token-counts-allowlist.md) | **Redaction-allowlist для usage-token-каунтов** (`*_tokens` ≠ секрет). Снимает конфликт docs↔docs: денилист `*token*` ([05-security.md](../05-security.md#логирование-безопасное)) непреднамеренно редактировал usage-каунты `input_tokens`/`output_tokens`/`total_tokens` (+ camelCase `inputTokens`/… в `ledger.meta.usage`), что нарушало требование сохранять usage в audit ([ADR-047 §6](ADR-047-usage-based-billing-for-agent.md), [agent-proxy/05-events.md](../modules/agent-proxy/05-events.md), реконсиляция [TD-029](../100-known-tech-debt.md)/[Q-047-2](../99-open-questions.md)). Решение: closed-set allowlist (6 lowercased имён) исключается из redaction, проверяется **первым** в `_is_sensitive_key` (как carve-out `*status*`). Денилист **не ослаблен**: реальные токен-секреты (`identityToken` [ADR-043](ADR-043-sign-in-with-apple.md), `push_token`, `x-admin-token` [ADR-009](ADR-009-admin-token-auth.md), `access_token`/`bearer`/`api_token`) редактируются как прежде. needs_code_sync: `src/app/observability/redaction.py` | Accepted | 2026-06-23 |
| [ADR-050](ADR-050-redaction-idempotencykey-allowlist.md) | **Redaction-allowlist для `idempotencyKey`** (дедуп-ключ ≠ секрет) — тот же приём, что [ADR-049](ADR-049-redaction-usage-token-counts-allowlist.md). Денилист `*key*` ([05-security.md](../05-security.md#логирование-безопасное)) редактировал `idempotencyKey`, который audit `admin_grant`/`admin_subscription_grant` **обязаны** нести для трассируемости ([ADR-048 §2](ADR-048-admin-credits-and-subscription-grant.md), [admin/06-rbac.md](../modules/admin/06-rbac.md)). `idempotencyKey` — клиентский дедуп-ключ ([ADR-005](ADR-005-idempotency-ledger.md)), НЕ секрет. Решение: closed-set allowlist `{idempotencykey, idempotency_key}` исключён из redaction, проверяется **первым** в `_is_sensitive_key` (объединён с usage-allowlist ADR-049). Денилист **не ослаблен** и **усилен**: попутно закрыт пре-существующий gap `encrypted_dek` (добавлен в `_DENY_EXACT`; до этого не редактировался) — defense-in-depth к [ADR-003](ADR-003-byok-envelope-encryption.md). needs_code_sync: `src/app/observability/redaction.py` (idempotencyKey allowlist + `encrypted_dek` в `_DENY_EXACT`) | Accepted | 2026-06-23 |
| [ADR-051](ADR-051-agent-debt-reconciliation.md) | **Реконсиляция несписанной дельты агентного прогона** (расширяет [ADR-047 §4/§6](ADR-047-usage-based-billing-for-agent.md), закрывает [TD-029](../100-known-tech-debt.md)). Гибрид (c)+(b): новая колонка `wallets.debt BIGINT NOT NULL DEFAULT 0 CHECK(>=0)` (миграция `0014`); на `InsufficientCredits` агентного `run.completed` `consume` списывает доступный `balance` (частично) и **недобор → `wallets.debt`** (audit `billing_debit_insufficient` + `debtAdded`/`partialDebited`); clawback при `WalletService.grant` (`repaid=min(grant,debt)` до увеличения `balance`, audit `billing_debt_repaid`); policy-блок `/v1/agent/run` при `debt>0` → `blocked`/`blockReason=debt_outstanding`. Флаг `AGENT_DEBT_RECONCILE_ENABLED` (дефолт `true`). Ledger остаётся чистым (`debt` — отдельный агрегат, не ledger-строка); pre-debit hold отвергнут; `balance>=0` CHECK сохранён. Закрывает [Q-047-2](../99-open-questions.md) в части реконсиляции | Accepted | 2026-06-24 |
| [ADR-052](ADR-052-durable-subscription-idempotency.md) | **Durable idempotency-якорь subscription-grant** (расширяет [ADR-048 §2](ADR-048-admin-credits-and-subscription-grant.md), закрывает [TD-030](../100-known-tech-debt.md)). Новая таблица `subscription_grant_events` (UNIQUE `(user_id, idempotency_key)` + `payload_hash` = sha256 `plan|expiresAt|grantCredits`, миграция `0015`, цепочка `0014`→`0015`). Единый алгоритм для **обоих** `grantCredits` (паттерн `adapty_webhook_events`, одна транзакция, `ON CONFLICT DO NOTHING`): тот же ключ + тот же `payload_hash` → `idempotentReplay=true`; тот же ключ + другой payload → **строгий `409`** (раньше для `grantCredits=false` был later-writer-wins). Двойная UNIQUE-граница при `grantCredits=true` (ledger + events). Request/Response не меняются; меняется только поведение идемпотентности `grantCredits=false`. Закрывает [Q-029-2](../99-open-questions.md)? нет — это TD-030; см. ADR-029 ревизию для TD-021 | Accepted | 2026-06-24 |
| [ADR-053](ADR-053-audit-logs-db-append-only.md) | **Durable append-only `audit_logs`** (закрывает [TD-001](../100-known-tech-debt.md)). Defense-in-depth: (1) runtime-роль БД `app_rw` (least-privilege, `GRANT INSERT,SELECT`, **`REVOKE UPDATE,DELETE,TRUNCATE`**) отдельно от миграционной `app_migrate`; (2) BEFORE-триггер `audit_logs_no_mutate()` на UPDATE/DELETE (`RAISE EXCEPTION`, миграция `0016`, цепочка `0015`→`0016`). Требует разведения runtime/migration ролей (два DSN — devops, [07-deployment.md](../07-deployment.md)). Erasure — out-of-band под привилегированной ролью с `DISABLE TRIGGER`. Распространение на `ledger_transactions`/hash-chain → [Q-053-1](../99-open-questions.md). Применяется только к `audit_logs` | Accepted | 2026-06-24 |
| [ADR-056](ADR-056-provision-readiness-gate-and-volume-ownership.md) | **Provision readiness-gate (cold-start) + согласование владения томом** (расширяет/уточняет [ADR-046](ADR-046-per-user-hermes-runtime.md) §1/§3/§5/§6; увязан с [TD-031](../100-known-tech-debt.md)). Живой e2e `/v1/agent/run` вскрыл два дефекта: **(A) cold-start readiness gap** — `_provision_locked` помечал `running` сразу после `docker run`, а образ (~5.3 GB) бутится ~30–40 с → прокси бьёт в неготовый `api_server:8642` → `502` + неконсистентная `running`-строка; **(B) volume ownership conflict** — api (uid 10001) пишет `config.yaml`, затем Hermes-образ (s6 stage2) `chown`'ит `/opt/data` на `HERMES_UID=10000` → reuse-`provision` падает `PermissionError(13)`. Решение **A**: readiness-poll `health(endpoint, api_key)` (Bearer, reuse) **после** `docker run`, **до** `mark_running`; интервал `HERMES_PROVISION_READY_INTERVAL_SECONDS` (2с), бюджет `HERMES_PROVISION_READY_TIMEOUT_SECONDS` (90с); таймаут → `remove`+cleanup строки (нет залипшей `running`/`provisioning`) → `502`; конкурентный `ensure_running` на свежей `provisioning` **ждёт** готовности, не перепровижинит; инвариант `HERMES_PROVISIONING_STALE_SECONDS(120) > ready(90)` (fail-fast в config). Readiness-poll живёт **после** commit'а `provisioning`-строки (арбитр гонки) — совместимо с коротко-транзакционным registry-паттерном (не удержание lock через docker run, ср. [ADR-054](ADR-054-trial-claim-reconcile.md) MAJOR-4). Решение **B** (вариант 1+3): прокинуть Hermes env `HERMES_UID`/`HERMES_GID` = uid/gid api (дефолт 10001) → stage2 chown тома на ТОТ ЖЕ uid → конфликта нет; idempotent-write `config.yaml` (не перезаписывать существующий валидный при reuse, только первый provision / full replay). needs_code_sync: `manager.py`/`docker_backend.py`/`config.py` | Accepted | 2026-06-24 |
| [ADR-055](ADR-055-hermes-instance-llm-config-contract.md) | **Контракт конфигурации LLM Hermes-инстанса** (расширяет/частично-ревизует [ADR-046](ADR-046-per-user-hermes-runtime.md) §1/§5/§6, фикс реального e2e 401 «Provider: openrouter, Model: (пусто)»). Корень: `render_instance_config` НЕ задавал секцию `model` → инстанс брал встроенный дефолт cli-config (`provider:auto`+openrouter base_url) → 401; `LLM_MODEL` env Hermes **игнорирует** (модель только из `config.yaml model.*`); дефолт-провайдер `openai` **невалиден** (нет direct-провайдера). Решение: (1) новая сигнатура `render_instance_config(*, toolset, provider, model, base_url="")` эмитит `model.default="<provider>/<model>"`+`model.provider="<provider>"` (КОНКРЕТНЫЙ, НЕ `auto`) [+`base_url` для custom/azure-foundry/lmstudio], safe-charset валидация значений (как toolset); (2) closed-set allowlist провайдеров (`openai` невалиден, `auto` запрещён для провижининга) + **fail-fast** в `_require_provision_config` (невалидный provider / пустой `HERMES_MODEL` / отсутствующий base_url → `UpstreamError` до `docker run`, не 401 в рантайме); (3) `HERMES_MODEL` = **«голое» имя модели** (control plane собирает `<provider>/<model>`); (4) env-набор: **убрать `LLM_MODEL` и `LLM_PROVIDER`** (игнорируются/дублируют config.yaml), ключ через явную map провайдер→key-env (`anthropic→ANTHROPIC_API_KEY`, `gemini→GOOGLE_API_KEY`, `huggingface→HF_TOKEN`, …), `HERMES_LLM_BASE_URL`→`model.base_url`. Дефолт `hermes_llm_provider` → `anthropic`. (5, ревизия 2026-06-25, §6 — закрытие [Q-055-1](../99-open-questions.md)): провайдеры **без env-ключа** (`custom`, набор `HERMES_PROVIDERS_CONFIG_API_KEY`) — ключ через `config.yaml model.api_key="${HERMES_INSTANCE_LLM_KEY}"` (env-ссылка, плейнтекст не в томе), env `HERMES_INSTANCE_LLM_KEY` вместо бесполезного `<PROVIDER>_API_KEY`; `render_instance_config` получает доп. параметр `api_key`; `model.default` для custom = `"custom/<model>"`. needs_code_sync: `config_yaml.py`/`manager.py`/`config.py` | Accepted | 2026-06-24 (ревизия §6: 2026-06-25) |
| [ADR-057](ADR-057-dedicated-server-self-hosted-traefik-deploy.md) | **Deploy-топология: ВЫДЕЛЕННЫЙ сервер `87.239.135.156` (avorelio.shop) с self-hosted Traefik-контейнером** в нашем `docker-compose.prod.yml` (НЕ внешний `/opt/edge`) — второй deploy-target для `claude-hermes`, **дополняет** [ADR-017](ADR-017-shared-server-traefik-deploy.md) (broadnova `.154` за внешним Traefik). Наш Traefik держит 80/443, авто-TLS Let's Encrypt (certResolver `le`, HTTP-01, acme.json в named volume, `ACME_EMAIL`), static-конфиг = CLI-флаги в `command:`, dashboard `--api=false`. Сеть `web` — **внутренняя** compose (на выделенном сервере нет внешнего совладельца); `hermes-net` остаётся `external` (инвариант [ADR-046](ADR-046-per-user-hermes-runtime.md)). `docker.sock :ro` читают ДВА сервиса (traefik provider + api provision) — риск ≈ root, митигация socket-proxy как задел. Preview pass-through `/v1/preview/*` ([ADR-010](ADR-010-backend-hosted-preview.md)) сохранён (исключён из глобальных header-middleware). `TRUSTED_PROXY_IPS` = подсеть нашей `web`. CI/CD push main → SSH `.156` (отдельный `SSH_HOST_AVORELIO`), `.env` на сервере. Инварианты [ADR-046](ADR-046-per-user-hermes-runtime.md)/[ADR-055](ADR-055-hermes-instance-llm-config-contract.md)/[ADR-056](ADR-056-provision-readiness-gate-and-volume-ownership.md)/[ADR-053](ADR-053-audit-logs-db-append-only.md) без изменений. `COVERAGE_CORE=sysmon` — Linux-CI no-op (проверено). Двойственность prod-файла — overlay `.156` с `networks.web.external: false` ([Q-057-1](../99-open-questions.md) Closed). **Ревизия 2026-07-15: `.156` — ЕДИНСТВЕННЫЙ deploy-target `claude-hermes` (broadnova INSTANCES-loop удалён из CI, §7/co-located `.154` — исторический контекст); фактический overlay = `docker-compose.avorelio.yml`. Ревизия (3) → [ADR-065](ADR-065-patched-hermes-image-ghcr.md): источник Hermes-образа = ghcr.io/eliseiv (наш патч, digest-pin), pull на `.156`, ghcr-доступ (public default).** | Accepted (ревизия 2026-07-15) | 2026-06-25 |
| [ADR-054](ADR-054-trial-claim-reconcile.md) | **Сериализация trial: claim-before-generation + reconcile-on-failure** (расширяет [ADR-002 §Trial concurrency](ADR-002-access-policy-state-machine.md), закрывает [TD-006](../100-known-tech-debt.md)). Исходное ТЗ TD-006 (advisory lock в generate-транзакции) нереализуемо — `orchestrator.py:962` делает `commit()` до LLM-вызова → xact-lock освобождается до flip (MAJOR-4, нулевая сериализация). Решение: trial **застолбляется** атомарным условным `UPDATE users SET trial_used=TRUE WHERE trial_used=FALSE RETURNING` в **отдельной короткой транзакции до генерации** (affected=0 → второй параллельный запрос → `blocked/trial_used`); при неуспехе генерации (`max_tokens`/upstream/exception) claim **откатывается** (`UPDATE ... SET trial_used=FALSE WHERE trial_used=TRUE` + флаг `claimed_trial`), сохраняя семантику «trial сгорает только при успехе» ([ADR-025](ADR-025-parallel-tool-calls-and-max-tokens-truncation.md)). Generate-loop/commit-до-LLM (MAJOR-4) не трогаются. Остаточный риск — «потерянный trial» при краше в окне claim↔reconcile (редкий, направлен в сторону сервиса/анти-абуза; [Q-054-1](../99-open-questions.md)). needs_code_sync: orchestrator trial-ветка | Accepted | 2026-06-24 |
| [ADR-058](ADR-058-x-user-id-string-identity.md) | **`X-User-Id` принимает произвольную непустую строку** (расширяет [ADR-044 §1/§3](ADR-044-client-api-key-auth.md)). `get_current_user` (`src/app/deps.py` 111–116) резолвит субъект детерминированно: пустой/whitespace → `401`; строка в UUID-форме → используется КАК ЕСТЬ (обратная совместимость с существующими пользователями/admin-грантами); иначе → `uuid.uuid5(HERMES_USER_NAMESPACE, raw)` (стабильный UUIDv5). `users.id` остаётся UUID PK, ~15 FK не трогаются, **миграций НЕТ**. `HERMES_USER_NAMESPACE=638724f1-2181-4d61-96de-5b8eedf7ef7a` — **код-константа**, immutable после запуска (смена = сдвиг всех производных id). Lazy provisioning ([ADR-007](ADR-007-lazy-user-provisioning.md)) и модель доверия ([ADR-044 §3](ADR-044-client-api-key-auth.md)) без изменений. needs_code_sync: `deps.py` резолвинг + namespace-константа, `openapi_security.py` описание `userId` | Accepted | 2026-07-06 |
| [ADR-059](ADR-059-hermes-only-openapi-surface.md) | **Hermes-only публичная OpenAPI-поверхность** (расширяет [08-api-documentation.md](../08-api-documentation.md) R4/R6). Legacy-роутеры (`chat`, `chats`, `workspaces`, `tools`, `models`, `presets`, `profile`, `preferences`, `preview`) регистрируются с `include_in_schema=False` в `src/app/main.py` (~301–320): скрыты из `/docs` и `/openapi.json`, но **маршруты активны и функциональны** (документационное изменение, не поведенческое). Видимая поверхность = Hermes-контуры: `agent`, `admin`, `wallet`, `policy`, `billing_adapty`, `token_purchase`, `byok` + `health`. `_OPENAPI_TAGS` сокращается до `Agent, Policy, Wallet, Tokens, BYOK, Billing (Adapty), Admin, Health` (теги `Auth` + legacy убраны; добавлено описание `Billing (Adapty)`). `title=claude-hermes` (R6 приводится в соответствие). needs_code_sync: `main.py` `include_in_schema` + `_OPENAPI_TAGS`. **РЕВЕРСИРОВАН 2026-07-06 (§7): поверхность OpenAPI снова полная (весь активный API минус retired `/v1/auth/*`); legacy-роутеры снова видимы (снят `include_in_schema=False`); полный `_OPENAPI_TAGS` восстановлен (минус `Auth`, `Billing (Adapty)` между `BYOK` и `Admin`).** | **Reversed (2026-07-06, §7)** | 2026-07-06 |
| [ADR-060](ADR-060-workspace-upload-transport-limit-alignment.md) | **Transport-лимит `SizeLimitMiddleware` согласован с contract-лимитом workspace-upload** (фикс прод-бага `413` на 484 KB). inline base64 раздувает тело ~4/3 + envelope → общий `SIZE_LIMIT_BODY` (512 KB) отрезал `413` уже с ~380 KB, делая контракт [ADR-036](ADR-036-workspaces-implementation.md) (8 MB/файл) недостижимым. Вводится независимая config-константа `WORKSPACE_REQUEST_BODY_LIMIT` (дефолт **12 MB**, покрывает base64(8 MB) ≈ 10.67 MB + envelope); `_limit_for` переводится с хардкода одного пути на **набор правил**: `/v1/chat/run` → `ATTACHMENT_REQUEST_BODY_LIMIT` (ADR-020, без изменений); `startswith("/v1/workspaces/") ∧ endswith("/files")` → `WORKSPACE_REQUEST_BODY_LIMIT` (НЕ матчит `/{id}/files/{file_id}`, create, detail); иначе → 512 KB. Вариант A (отдельная константа) предпочтён B (переиспользовать attachment-лимит) — независимый контроль поверхности. Инвариант: transport ≥ base64(contract) × 4/3 + envelope. Агрегатный кейс chat/run (10 MB total → 13.33 MB base64 > 12 MB) вынесен в [TD-033](../100-known-tech-debt.md). needs_code_sync: `config.py` (новая константа), `middleware.py` (`_limit_for` ruleset). | Accepted | 2026-07-15 |
| [ADR-062](ADR-062-wake-readiness-gate-and-connect-only-launch-retry.md) | **Readiness-gate на wake-пути + connect-only retry запуска run** (фикс прод-бага: `POST /v1/agent/run` `502` ~каждый 3–4-й запрос после гибернации). Корень — readiness-gate [ADR-056](ADR-056-provision-readiness-gate-and-volume-ownership.md) реализован ТОЛЬКО в `_provision_locked`, а ветка wake (`stopped → running`) в `ensure_running` осталась БЕЗ gate (`start` → немедленный `mark_running`) — код расходится с уже задокументированным потоком `03-architecture.md` (`S → readiness-poll → mark_running`); `POST /v1/runs` уходит ДО `TCPSite.start()` api_server → transport error → `502`. **B (корень):** распространить readiness-poll `health=200` на wake коротко-транзакционно (`T=now`→`start`→`mark_provisioning(provisioning_started_at=T)` COMMIT арбитр→poll `_wait_for_ready` без DB→`mark_running`), reuse статуса `provisioning` (без ALTER enum), reuse бюджета `HERMES_PROVISION_READY_*`. **§1a (critical, review):** stale-якорь `_is_stale_provisioning` переведён с `created_at` (иммутабельно; для разбуженного инстанса = исходное создание → живая wake-попытка ложно stale → конкурентный replay сносит её in-flight контейнер, арбитр недостижим) на **новую колонку `provisioning_started_at`** (миграция `0017`, expand-only, backfill=`created_at`). **§1b (major, review):** wake-таймаут — **условный** `mark_stopped_if_provisioning(user_id,T)` (guard `status='provisioning' AND provisioning_started_at=T`; rowcount=0→не трогаем строку/контейнер — анти-clobber конкурентно поднятого `running`). Сигнал `/health` достаточен: маршруты `/health`+`/v1/runs` образа Hermes на одном aiohttp-listener, регистрируются ДО `TCPSite.start()` → `/health=200` ⟺ `/v1/runs` маршрутизируем. **A (страховка):** connect-only retry `_launch_run` (`HERMES_LAUNCH_RETRY_ATTEMPTS`=3, backoff `HERMES_LAUNCH_RETRY_BACKOFF_SECONDS`=2.0) ТОЛЬКО на connect-фазе явным кортежем `isinstance(exc,(httpx.ConnectError,httpx.ConnectTimeout,httpx.PoolTimeout))` (httpx 0.28.1: `ConnectTimeout` НЕ подкласс `ConnectError`, базы over-catch'ат post-send); post-send (`ReadError`/`ReadTimeout`/`WriteError`/`RemoteProtocolError`) НЕ retry (`POST /v1/runs` не идемпотентен → дубль-run). C (idle-timeout) отклонён. needs_code_sync: `hermes_runtime/manager.py` (`ensure_running` wake-gate + `_is_stale_provisioning` re-anchor), `hermes_runtime/registry.py` (`mark_provisioning`/`mark_stopped_if_provisioning`/`create_provisioning`), `models/tables.py`+миграция `0017`, `agent_proxy/service.py` (`_launch_run` retry), `config.py` (2 retry-поля). | Accepted | 2026-07-15 |
| [ADR-061](ADR-061-admin-wallet-debit.md) | **Admin-эндпоинт списания баланса `POST /v1/admin/wallet/debit`** (закрывает прод-пробел: коррекция баланса делалась прямым SQL). Выбрана семантика **debit-на-дельту** (вариант A), НЕ set-абсолют (B): durable-идемпотентность есть только у debit (ledger `idempotency_key`; у set-абсолюта дельта от live-баланса — нет durable-якоря), симметрия с `credits/grant`, отсутствие конфликта с clawback долга ([ADR-051](ADR-051-agent-debt-reconciliation.md); `grant` гасит долг → `balance ≠ targetBalance`). Тело `{userId, amount>0, idempotencyKey, reason}` → `{newBalance, ledgerTxId, idempotentReplay}`. Реюз `WalletService.consume(meta.source="admin_debit", session_id=None)` — **без нового метода сервиса, без миграции**. `amount > balance` → `409 insufficient_credits` (savepoint-откат, НЕ clamp). `meta.source="admin_debit" ≠ "agent_run"` → `_agent_reconcile_applies=False` → `wallets.debt` не затрагивается (инвариант долга сохранён). Новый `AdminService.debit` + audit `admin_debit` + метрика `admin_debit_total`. Контур `adminToken`/`X-Admin-Token` ([ADR-009](ADR-009-admin-token-auth.md), неизменен), тег Admin, body cap ≤ 8 KB. Коррекция самого долга — вне scope ([Q-061-1](../99-open-questions.md)). needs_code_sync: `schemas/admin.py` (`AdminDebitRequest`), `admin/service.py` (`debit`), `api_gateway/routers/admin.py` (route), `audit/service.py` (`EVENT_ADMIN_DEBIT`), `observability/metrics.py` (`admin_debit_total`). | Accepted | 2026-07-15 |
| [ADR-066](ADR-066-agent-run-state-snapshot.md) | **Снапшот состояния agent-прогона `GET /v1/agent/runs/{runId}/state`** (расширяет [ADR-064](ADR-064-incremental-agent-run-billing-and-pause-resume.md): `agent_runs` из «строки под флагом биллинга» становится **безусловной lifecycle-записью**, чинятся никогда-не-писавшиеся `failed`/`cancelled`; **все** переходы статуса условные `WHERE status IN ('running','resumed')`, `mark_stopped(run_id, user_id)` — **owner-scoped** (`AND user_id=:uid`) и только на клиентском `POST /stop`, внутренний interrupt pause-at-zero его не выполняет; терминальный статус пишет единый `_mark_terminal(run_id, status)` **до** биллинга и независимо от его исхода — закрывает «вечный `running`» при отказе списания). **Проблема:** `/events` — чистый SSE-relay без персистенции ([ADR-045](ADR-045-hermes-as-agent-proxy.md)), накопленный текст живёт в локальных переменных генератора; инстанс Hermes гибернируется (`HERMES_IDLE_TIMEOUT_SECONDS`, [ADR-046](ADR-046-per-user-hermes-runtime.md)) и теряет in-memory реестр прогонов → после kill приложения клиент получает `200` без событий. **Решение (v1):** снапшот пишется **побочным эффектом существующего relay** `stream_events` в новую таблицу **`agent_run_snapshots`** (1:1 к `agent_runs`, миграция `0019`, цепочка `0018`→`0019`, [03-data-model.md §25](../03-data-model.md)): `result_text` (head-preserving truncation `AGENT_STATE_RESULT_TEXT_MAX_CHARS`=64 KB — общий потолок для `result_text`, `pending_approval.preview` и `output` в `run.paused`; троттлинг `AGENT_STATE_FLUSH_INTERVAL_SECONDS`=3.0 с, **per-column replay-guard** — `result_text` через **префиксный** `CASE … char_length(EXCLUDED) >= char_length(t) AND left(EXCLUDED, char_length(t)) = t` (длины мало — пропустила бы более длинную подмену; при no-replay текст замерзает вместо порчи), токены через `GREATEST`, `pending_approval` — через `CASE WHEN :assert_approval` (значение утверждают только три call-site апсерта — `approval.request`/`tool.*`/терминальные; троттлинговый `message.delta` идёт с `false` и поле не трогает — иначе воскрешал бы снятый approval; ответ `POST …/approval` снимает approval отдельным owner-scoped `clear_pending_approval(run_id,user_id)` — не апсерт), `last_tool`/`updated_at` — безусловно; **row-level `WHERE` у `DO UPDATE` запрещён** (гейтил бы строку целиком: в replay-окне не записался бы `pending_approval` → `waiting_approval` не отдался бы никогда)), `last_tool`, `pending_approval JSONB {tool,preview}` (событие `approval.request` ранее нигде не сохранялось — server-side «ждём подтверждения» не существовало), токены (`GREATEST`). Статус **не дублируется** — источник истины остаётся `agent_runs.status`; enum `agent_run_status` **не меняется**. **Эндпоинт строго read-only:** без `ensure_running` (контейнер НЕ будится), без обращения к Hermes, без списаний, без policy-gate; auth `clientApiKey`+`userId` ([ADR-044](ADR-044-client-api-key-auth.md)), RBAC-**404** (никогда 403), `429` по общему rate-limit; ответ camelCase `{runId, sessionId, status, resultText, lastTool, pendingApproval{tool,preview}, blockReason, usage{inputTokens,outputTokens}, updatedAt, continuedFrom}`. **Маппинг статусов** server→client: `running`/`resumed`→`running`; `+pending_approval`→**`waiting_approval`** (производный, НЕ значение enum); `paused`→`paused` (+`blockReason` = `paused_reason`, НЕ policy-enum [ADR-004](ADR-004-blocked-http-200.md)); `completed`→`completed`; `failed`→`failed`; `cancelled`→**`stopped`**; `queued` — в `Literal` для forward-compat, в v1 не эмитится. **Retention `AGENT_RUN_SNAPSHOT_TTL_DAYS`=14** — чистится только `result_text`/`pending_approval` терминальных прогонов, строка остаётся (`/state` продолжает отдавать статус+usage); sweep **идемпотентен** (guard `AND (result_text <> '' OR pending_approval IS NOT NULL)`) и **не двигает `updated_at`**. **[Q-047-2](../99-open-questions.md) НЕ закрыт:** снапшот привязан к тому же relay, что и биллинг (нет подписчика — нет ни списаний, ни обновлений снапшота); фоновый consumer (закрыл бы обе проблемы) — **отложен** (§Alternatives 1). **Q-066-1** (семантика replay Hermes при re-subscribe — подтвердить при патче образа/E2E), **Q-066-2** (допущение о глобальной уникальности Hermes `run_id` при глобальном `TEXT PK` + tenancy defense-in-depth), **Q-066-3** (`continuedTo` parent→child — кандидат следующей ревизии). Ограничения: `resultText` активного прогона отстаёт без подписчика (детект по `updatedAt` → переподключиться к `/events`); прогоны, запущенные до деплоя → `404` (backfill не делается); `stop`→`stopped` — eventual consistency; после resume родительская строка навсегда `resumed`→клиентский `running` с замороженным `updatedAt` (рецепт — повторный идемпотентный `POST …/resume`: резолв `active_child`). Row-level **tenancy-гвард** `WHERE t.user_id = EXCLUDED.user_id` + owner-scoped `get(run_id,user_id)` — гейт по identity, запрету staleness-`WHERE` не противоречит. Sweep — под **частичным** индексом `ix_agent_run_snapshots_sweep` (полный по `updated_at` не заводится: write-amplification без читателя; итого 2 индекса). needs_code_sync: `models/tables.py` + миграция `0019`, `agent_proxy/snapshots_repo.py` (новый), `agent_proxy/runs_repo.py` (`mark_status(run_id,status)` + owner-scoped `mark_stopped(run_id,user_id)`), `agent_proxy/snapshots_repo.py` (upsert + `clear_pending_approval(run_id,user_id)`), `agent_proxy/service.py` (снапшот-writer в `stream_events`, развязка `create_running`/`_mark_terminal` от флага, очистка `pending_approval` в `approval()`, retention-sweep в reaper), `api_gateway/routers/agent.py` (роут `/state`), `schemas/agent.py` (`AgentRunStateResponse`+`AgentPendingApproval`), `config.py` (3 настройки), `deps.py` | Accepted | 2026-07-28 |
| [ADR-065](ADR-065-patched-hermes-image-ghcr.md) | **Патченый образ Hermes-runtime из ghcr.io (`usage.delta`), digest-pinned, off-server сборка** (ревизует источник `HERMES_IMAGE` из [ADR-046](ADR-046-per-user-hermes-runtime.md)/[ADR-057](ADR-057-dedicated-server-self-hosted-traefik-deploy.md); реализует зависимость образа [ADR-064 §7](ADR-064-incremental-agent-run-billing-and-pause-resume.md)). Upstream `nousresearch/hermes-agent` **не** несёт per-step `usage.delta` + hydrate — нужен **наш** патч (verified `agent/conversation_loop.py:2061` + `gateway/platforms/api_server.py:4485`), а push в чужой namespace невозможен. **Решение:** `HERMES_IMAGE = ghcr.io/eliseiv/hermes-agent@sha256:<digest>` (**digest-pin**, НЕ `:latest`), собирается **off-server** (dev/CI) + push ghcr + pull на `.156` (инвариант «no build on server» сохранён — дерево Hermes на prod не попадает). **Версионирование патча — Вариант B:** diff-патч `infra/hermes-patches/usage-delta.patch` в репо claude-hermes, применяемый на pinned upstream-базе при сборке (фолбэк A — snapshot-форк, если база не пиннится чисто). **ghcr-доступ:** default **публичный** пакет (анонимный pull, нет секрета на `.156`; MIT-лицензия Hermes позволяет) / альтернатива private + `docker login read:packages` под root. Лицензия Hermes — MIT (verified). Maintenance-долг синхронизации патча с upstream — [TD-036](../100-known-tech-debt.md); visibility+base-pin — [Q-046-4](../99-open-questions.md). needs_infra_sync (devops): `infra/hermes-patches/usage-delta.patch` (новый), build-recipe/Dockerfile off-server, `docker-compose.e2e.hermes.yml` (image ref), опц. CI publish-workflow. | Accepted | 2026-07-15 |
| [ADR-064](ADR-064-incremental-agent-run-billing-and-pause-resume.md) | **Пошаговый биллинг agent-run + pause-at-zero + resume** (расширяет/частично-ревизует [ADR-047](ADR-047-usage-based-billing-for-agent.md) §2/§4, [ADR-045](ADR-045-hermes-as-agent-proxy.md) SSE, сужает область [ADR-051](ADR-051-agent-debt-reconciliation.md)). Под флагом `agent_incremental_billing_enabled` (OFF = постфактум [ADR-047](ADR-047-usage-based-billing-for-agent.md)). **Тарификация «cumulative-owed минус charged» в КРЕДИТАХ:** на каждом `usage.delta` (Hermes-патч, несёт `cumulative_*_tokens`) `owed=usage_to_credits(cum)`, `want=owed−charged`, `charge=min(want,balance)` — телескопирует **точно** в `usage_to_credits(final)` (per-step `ceil` дельт ЗАПРЕЩЁН — инфляция). Per-step idempotency `runId:step` + финализация остатка на `run.completed` (idempotency `runId`). **Pause-at-zero:** при `charge<want` → `stop`→Hermes interrupt + синтетическое терминальное SSE `run.paused` (reason `credits_exhausted`, промежуточный JSON из relay-буфера) + `agent_runs.status=paused`; **БЕЗ долга** (`charge≤balance`→debt-ветка [ADR-051](ADR-051-agent-debt-reconciliation.md) недостижима; `netBalance==balance` [ADR-063](ADR-063-client-facing-debt-and-net-balance.md)). No-debt consume — **выделенная третья ветка** `_consume_incremental_clamp` (маршрутизация по `meta.incremental` ДО `_debit_in_savepoint`; `LEAST(:amount,balance)`, без raise/без debt — иначе savepoint-ветка подняла бы `InsufficientCreditsError`, порвав стрим); debt остаётся fallback (finalization/race/flag-off). Seed `charged` из ledger (`SUM` по `runId OR runId:%`, `ESCAPE '\'`, reconnect-safe). **Resume `POST /v1/agent/runs/{runId}/resume {message?}`:** RBAC-404, пред-гвард `status∈{paused,resumed}`→409 `run_not_resumable`, policy-gate (blocked 200, до флипа), **атомарный CAS `paused→resumed`** (арбитр гонки — конкурентные in-flight resume/ретраи не создают два child; проигравший→active child или `409 resume_in_progress`), hydrate `GET {base}/api/sessions/{sessionId}/messages`→`conversation_history` (`404`/пусто→`409 session_expired`, откат CAS; transport/non-2xx→`502`), `_launch_run` **после** CAS (single-flight, `POST /v1/runs` неидемпотентен)+chain `create_running(continued_from_run_id)`, `202 {runId:new, continuedFrom:old}`; свежий keyspace `newRunId:step`. Стабильный `session_id=body.sessionId or uuid4()` пишется в `agent_runs`. **Таблица `agent_runs`** (миграция `0018`, цепочка `0017`→`0018`): `run_id` PK, `user_id` FK, `session_id`, `status` enum(6), `cumulative_credits_spent`, `last_billed_step`, `paused_reason`, `continued_from_run_id` self-FK, `model`. Аддитивно `AgentRunResponse.continuedFrom`. Внешний Hermes-контракт: `usage.delta` (патч образа) + hydrate-endpoint. Q-064-1..5. needs_code_sync: `models/tables.py`+миграция `0018`, `agent_proxy/runs_repo.py` (новый), `agent_proxy/service.py` (incremental+pause+finalize+resume+effective session_id), `wallet/service.py` (`charged_for_run`+no-debt consume), `api_gateway/routers/agent.py` (resume route), `schemas/agent.py` (`AgentResumeRequest`+`continuedFrom`), `config.py` (`agent_incremental_billing_enabled`) | Accepted | 2026-07-15 |
| [ADR-063](ADR-063-client-facing-debt-and-net-balance.md) | **Клиентское отображение долга — аддитивные `debt` + `netBalance` в `WalletResponse` и `EffectivePolicyResponse`** (закрывает пробел: `wallets.debt` из [ADR-051](ADR-051-agent-debt-reconciliation.md) виден только в admin-API, клиент видел долг как `balance=0`). Продуктовое требование: клиент **видит долг как отрицательный баланс**. Существующие `balance`/`creditsBalance` **не меняются** (`≥0`, обратная совместимость) — только **аддитивные** поля. Выбран вариант **оба поля** (не только `netBalance`): `debt: int ≥0` (размер долга, семантика **едина** с `AdminWalletResponse.debt`, [ADR-061](ADR-061-admin-wallet-debit.md)) для прозрачности блокировки `debt_outstanding`/clawback + `netBalance: int` (`= balance − debt`, может быть `<0`) для прямого показа отрицательного числа. Одинаковые имена в обоих ответах (клиент читает один ключ). Единый источник — колонка `wallets.debt`: wallet-путь через `WalletService.get_wallet_view` (уже возвращает debt), policy-путь читает ту же колонку в `effective()` (consistency `wallet↔policy` by construction). Инвариант: `AGENT_DEBT_RECONCILE_ENABLED=false → debt=0 → netBalance==balance` (держится на стороне записи, проверка флага в read-путях не нужна). `PolicyState` (чистый вход engine) **не меняется** — долг читается рядом с engine, не внутри. Без миграции (читается `wallets.debt`, миграция `0014` из ADR-051). needs_code_sync: `schemas/wallet.py` (`WalletResponse`), `schemas/policy.py` (`EffectivePolicyResponse`), `api_gateway/routers/wallet.py` (`get_wallet`), `policy/loader.py` (`EffectivePolicy`+`effective`), `api_gateway/routers/policy.py` (`policy_effective`). | Accepted | 2026-07-15 |

## Ревизии

- **ADR-064 (расширение 2026-07-28 → [ADR-066](ADR-066-agent-run-state-snapshot.md), развязка `agent_runs` от флага биллинга):** §6 читается так, что строка `agent_runs` создаётся **только** на incremental-пути (флаг `agent_incremental_billing_enabled`, дефолт OFF) — **устарело**. [ADR-066 §3](ADR-066-agent-run-state-snapshot.md) делает `create_running` и запись терминального статуса **безусловными** (узкий `_mark_completed` заменён единым `_mark_terminal(run_id, status)`, вызываемым **до** биллинга и независимо от его исхода — отказ списания больше не оставляет прогон вечно `running`) (строка — lifecycle-запись, пишется всегда, в т.ч. при флаге OFF); под флагом остаётся **только биллинг** (`record_step`, per-step debits, pause-at-zero, финализация остатка, resume-policy). Дополнительно начинают записываться статусы `failed` (на `run.failed`) и `cancelled` (условный owner-scoped `UPDATE ... WHERE user_id=:uid AND status IN ('running','resumed')` в `mark_stopped(run_id, user_id)` на 2xx клиентского `POST /stop`), которые до сих пор существовали в enum, но никогда не выставлялись. Тарификация, idempotency-keyspace, CAS-resume и no-debt инвариант §1–§5 — **без изменений**. Тело ADR-064 не переписано; пометка в шапке.
- **ADR-046 (ревизия 2026-07-15 → [ADR-065](ADR-065-patched-hermes-image-ghcr.md), источник `HERMES_IMAGE`):** §Context/§1 читаются как upstream публичный `nousresearch/hermes-agent` — **устарело**. Источник runtime-образа Hermes-инстансов меняется на **наш патченый `ghcr.io/eliseiv/hermes-agent@sha256:<digest>`** (digest-pin), т.к. incremental-биллинг ([ADR-064 §7](ADR-064-incremental-agent-run-billing-and-pause-resume.md)) требует `usage.delta` + hydrate, которых нет в upstream. Инварианты жизненного цикла/env/`config.yaml`/toolset/владения томом §1–§6 — без изменений (патч аддитивен). Тело ADR-046 не переписано; пометка в шапке.
- **ADR-057 (ревизия 2026-07-15 (3) → [ADR-065](ADR-065-patched-hermes-image-ghcr.md), источник образа + pull-модель `.156`):** §6 prod-checklist шаг 4 (`docker pull nousresearch/hermes-agent:<tag>`) читается как `docker pull ghcr.io/eliseiv/hermes-agent@sha256:<digest>`. Инвариант «build на сервере» применяется только к образу `api`/`migrate` (`claude-hermes`-код); runtime-образ Hermes собирается **off-server** и pull'ится (самодостаточность `.156` сохранена). ghcr-доступ: default публичный пакет (нет секрета на `.156`; MIT позволяет) / альтернатива private + `docker login read:packages`. Инварианты §1–§4/§6 без изменений. Тело ADR-057 не переписано; §Ревизия (3) добавлена.
- **ADR-064 (реализация зависимости образа 2026-07-15 → [ADR-065](ADR-065-patched-hermes-image-ghcr.md)):** «патч образа Hermes» §7 (`usage.delta` + hydrate) поставляется как патченый образ `ghcr.io/eliseiv/hermes-agent@sha256:<digest>` ([ADR-065](ADR-065-patched-hermes-image-ghcr.md)); патч verified (`conversation_loop.py:2061`+`api_server.py:4485`). Флаг `agent_incremental_billing_enabled` включается только после образа на `.156` + зелёного e2e. Тело ADR-064 не переписано; пометка в шапке.
- **ADR-047 (расширение/частичная ревизия 2026-07-15 → [ADR-064](ADR-064-incremental-agent-run-billing-and-pause-resume.md)):** [ADR-064](ADR-064-incremental-agent-run-billing-and-pause-resume.md) переводит биллинг агентного пути с **постфактум** (§2 `amount=usage_to_credits(final)` на `run.completed`, idempotency `runId`) на **incremental** (per-step на `usage.delta`, idempotency `runId:step`; `run.completed`→финализация остатка с idempotency `runId`). Схема «cumulative-owed минус charged» телескопирует **точно** в прежнюю сумму `usage_to_credits(final)` — §2-формула не отменена, применяется к кумулятиву. Под флагом `agent_incremental_billing_enabled`; OFF → тело ADR-047 без изменений (постфактум). §6 (data-integrity savepoint) сохранён. Тело ADR-047 не переписано (immutability); пометка в шапке.
- **ADR-045 (расширение 2026-07-15 → [ADR-064](ADR-064-incremental-agent-run-billing-and-pause-resume.md)):** [ADR-064](ADR-064-incremental-agent-run-billing-and-pause-resume.md) расширяет SSE-контракт [ADR-045](ADR-045-hermes-as-agent-proxy.md) §3 двумя событиями: **`usage.delta`** (Hermes-патч образа — per-step usage с `cumulative_*_tokens`, источник incremental-тарификации; внешний контракт Hermes-репо) и **`run.paused`** (генерирует control plane при исчерпании баланса — синтетическое терминальное событие). Добавлена зависимость от `GET {base}/api/sessions/{sessionId}/messages` (hydrate для resume). SSE-ретрансляция как есть; новые события аддитивны (старые клиенты игнорируют). Тело ADR-045 не переписано; пометка в шапке.
- **ADR-051 (сужение области 2026-07-15 → [ADR-064](ADR-064-incremental-agent-run-billing-and-pause-resume.md)):** [ADR-064](ADR-064-incremental-agent-run-billing-and-pause-resume.md) сужает область debt-реконсиляции ADR-051: на **incremental**-пути долг **не копится** (`charge=min(want,balance)≤balance` → условный UPDATE проходит → `InsufficientCreditsError`/`wallets.debt` недостижимы; при 0 — `run.paused` без долга). `wallets.debt`/clawback/`debt_outstanding` ADR-051 **остаются** как fallback для finalization на `run.completed` (редкая гонка `remainder>balance`) и legacy-ветки (флаг OFF = постфактум). No-debt incremental-consume (`meta.incremental`) обходит `_agent_reconcile_applies`. Согласовано с [ADR-063](ADR-063-client-facing-debt-and-net-balance.md): paused не двигает `debt` → `netBalance==balance`. Тело ADR-051 не переписано; пометка в шапке.
- **ADR-056 (расширение 2026-07-15 → [ADR-062](ADR-062-wake-readiness-gate-and-connect-only-launch-retry.md)):** [ADR-062](ADR-062-wake-readiness-gate-and-connect-only-launch-retry.md) распространяет readiness-gate §1/§2 ADR-056 на **wake-путь** `ensure_running` (`stopped→running`), который остался без gate (реализован был только `_provision_locked`) — код расходился с уже задокументированным потоком `modules/hermes-runtime/03-architecture.md`; следствие — прод-`502` `/v1/agent/run` после гибернации. Также уточняет допущение ADR-056 Alt-2 «`/health` — точный сигнал `api_server`»: подтверждено по коду образа (маршруты `/health`+`/v1/runs` на одном aiohttp-listener до `TCPSite.start()`), сигнал достаточен. Тело ADR-056 не переписано (immutability). Открытый: [Q-062-1](../99-open-questions.md).
- **ADR-017 (ревизия 2026-07-15 — для `claude-hermes` только выделенный `.156`, broadnova INSTANCES-loop удалён из CI):** `claude-hermes` — самостоятельный сервис на **выделенном** сервере `87.239.135.156` (`avorelio.shop`, [ADR-057](ADR-057-dedicated-server-self-hosted-traefik-deploy.md)). Раздел «Мульти-инстанс / клонирование» ADR-017 (`INSTANCES`-loop, `COMPOSE_PROJECT_NAME`-клонирование за общим edge-Traefik на `.154`) — **fork-артефакт claude-ios**, к деплою `claude-hermes` не применяется. Из `.github/workflows/ci.yml` + `deploy.yml` удалён deploy-job с broadnova `INSTANCES`-loop (деплоил чужие `claude-ios`/`avelyra`/`orvianix`/`veltrio` + дубль co-located `claude-hermes` на `.154`); остался **единственный** `deploy-avorelio` (`.156`, `SSH_HOST_AVORELIO`). Сервер-агностичные инварианты ADR-017 (label-контракт `api`, build-на-сервере, preview pass-through, `set -uo pipefail`/readiness-gate) переиспользуются на `.156`. Тело ADR-017 не переписано; ограничена область применимости для `claude-hermes`. Расписано в [ADR-017 §Ревизия 2026-07-15](ADR-017-shared-server-traefik-deploy.md#ревизия-2026-07-15--для-claude-hermes-применяется-только-выделенный-156-adr-057); синхронно обновлён [07-deployment.md](../07-deployment.md).
- **ADR-057 (ревизия 2026-07-15 (2) — split-up: `--force-recreate` ТОЛЬКО для `api`, фикс stale-code на зелёном деплое):** §5/§Rollback описывали старт как единый `up -d --no-build`. Прод-дефект: `docker compose up -d` решает пересоздание по **CONFIG-hash сервиса**, ссылающемуся на образ по **стабильному тегу** `claude-hermes-backend:prod` (не image id) → пересборка под тем же тегом не меняет hash → `up -d --no-build` оставляет старый контейнер `api` (старый код), хотя образ пересобран и миграция применена; `/ready` (db+redis) зелёный, т.к. не отражает версию кода → зелёный деплой на старом коде (новые API-поля → `null`). Фикс (devops, `ci.yml deploy-avorelio` + `deploy.yml`): единый `up` разделён на **(3a)** обычный `up -d --no-build --no-deps postgres redis traefik` (БЕЗ force-recreate) + **(3b)** `up -d --no-build --no-deps --force-recreate api` (безусловное пересоздание ТОЛЬКО api). **Инвариант деплоя:** `postgres`/`redis`/`traefik` НИКОГДА не под `--force-recreate` (данные / кэш / ACME-cert `acme.json` + владелец `:443`). Тот же split — в §Rollback. Тело ADR-057 не переписано (immutability). Follow-up на пост-деплой верификацию версии кода — [TD-034](../100-known-tech-debt.md). Расписано в [ADR-057 §Ревизия 2026-07-15 (2)](ADR-057-dedicated-server-self-hosted-traefik-deploy.md#ревизия-2026-07-15-2--split-up-force-recreate-только-для-api-фикс-stale-code-на-зелёном-деплое); синхронно обновлён [07-deployment.md §Процедура деплоя/§Откат](../07-deployment.md#процедура-деплоя-github-actions--ssh-156).
- **ADR-057 (ревизия 2026-07-15 — `.156` стал ЕДИНСТВЕННЫМ target; имя overlay-файла):** прежняя формулировка «второй/дополняющий broadnova `.154` deploy-target» пересмотрена: `.156` — **единственный** target `claude-hermes` (broadnova `INSTANCES`-loop удалён из CI, см. ревизию ADR-017 выше). §7 (совпадение имени project с co-located `.154`) и упоминания broadnova-loop — исторический контекст (co-located инстанса из этого репозитория больше нет). **Фактический overlay-файл — `docker-compose.avorelio.yml`** (не `docker-compose.traefik.yml`, как в immutable §3/§Последствия); механизм (`networks.web.external: false` + `name: web`) неизменен. Инварианты §1–§4/§6 без изменений. Тело ADR-057 не переписано. Расписано в [ADR-057 §Ревизия 2026-07-15](ADR-057-dedicated-server-self-hosted-traefik-deploy.md#ревизия-2026-07-15--156-стал-единственным-deploy-target-broadnova-instances-loop-удалён-имя-overlay-файла); синхронно обновлён [07-deployment.md](../07-deployment.md).
- **ADR-059 (ревизия 2026-07-06 — REVERSED, полная поверхность OpenAPI):** исходное решение «Hermes-only surface» (скрыть девять legacy-роутеров через `include_in_schema=False`) **отменено** по изменению требования: iOS-приложение использует НЕ только Hermes-контур (`/v1/agent/*`), но и claude-ios legacy (`chat`/`chats`/`workspaces`/`tools`/`models`/`presets`/`profile`/`preferences`/`preview`), и разработчику клиента эти эндпоинты нужны **видимыми в `/docs`**. Механизм — просто снятие `include_in_schema=False` (новый механизм не вводится); поверхность OpenAPI = **весь активный API минус retired `/v1/auth/*`** ([ADR-044 §4a](ADR-044-client-api-key-auth.md), issuer не смонтирован). Полный `_OPENAPI_TAGS` восстановлен в порядке `Agent, Chat, Tools, Models, Presets, Policy, Wallet, Tokens, BYOK, Billing (Adapty), Admin, Preview, Chats, Workspaces, Profile, Preferences, Health` (pre-059 порядок минус `Auth`, с `Billing (Adapty)` между `BYOK` и `Admin`; `Subscription` не возвращается — [TD-021](../100-known-tech-debt.md)). `title=claude-hermes` сохранён. Эндпоинты всё это время были рабочими (hide-only) — отмена чисто документационная, поведение/контракты/биллинг/миграции не затронуты. Расписано в [ADR-059 §7](ADR-059-hermes-only-openapi-surface.md); синхронно обновлён [08-api-documentation.md R4](../08-api-documentation.md). needs_code_sync: `src/app/main.py` (`include_in_schema` + `_OPENAPI_TAGS`).
- **ADR-046 (расширение/частичная ревизия 2026-06-24 → [ADR-055](ADR-055-hermes-instance-llm-config-contract.md)):** реальный e2e вскрыл интеграционный дефект конфигурации LLM Hermes-инстанса (401 «Provider: openrouter, Model: (пусто)»). [ADR-055](ADR-055-hermes-instance-llm-config-contract.md) уточняет §1 (env-проброс: убрать игнорируемый `LLM_MODEL` и `LLM_PROVIDER`) и §5/§6 (рендер `config.yaml`: добавить секцию `model.default`/`model.provider`[/`base_url`], запретить `provider:auto`, fail-fast валидация провайдера/модели). Тело ADR-046 не переписано (immutability); контракт `config.yaml`/env читается из ADR-055.
- **ADR-054 (расширение 2026-06-24 — §2a Continuation tool-loop под claimed trial):** при реализации [TD-006](../100-known-tech-debt.md) выявлен не покрытый §2 кейс на пути `/v1/chat/tool-result`: trial-ход с **client-side** инструментами возвращает `tool_call` (claim уже выполнен на `/chat/run`), и continuation после закрытия барьера ошибочно блокируется `blockReason=trial_used` своим же claim'ом (re-evaluate видит `trial_used=TRUE`; регрессия claim-before). Добавлен §2a: детерминированный признак in-progress trial-хода (`mode=credits ∧ subscription=none ∧ trial_used=TRUE ∧ assistant_tool_step_id(message_step_id)≠None`) — решается **в orchestrator** (policy остаётся чистой функцией); не-блокировка continuation своего trial-хода по `trial_used`; reconcile-откат при неуспехе continuation тем же признаком (замена request-scoped `claimed_trial`, который не переносится между запросами). Без миграции, публичный контракт `/chat/tool-result` не меняется. Тело §1–§4 не переписано (расширение добавлением раздела). Синхронно обновлён [modules/chat-orchestrator/03-architecture.md §Поток /v1/chat/tool-result](../modules/chat-orchestrator/03-architecture.md).
- **ADR-002 (расширение 2026-06-24 → [ADR-054](ADR-054-trial-claim-reconcile.md)):** [ADR-054](ADR-054-trial-claim-reconcile.md) расширяет §Trial concurrency — ранее «осознанно принятый риск» (окно двойной trial-генерации, [TD-006](../100-known-tech-debt.md)) **закрывается** claim-before-generation + reconcile-on-failure (исходное advisory-lock-ТЗ TD-006 нереализуемо из-за `commit()` до LLM, MAJOR-4). Policy Engine (чистая функция) не меняется; сериализацию даёт claim-UPDATE. Тело ADR-002 не переписано; пометка в §Trial concurrency. Закрывает [TD-006](../100-known-tech-debt.md).
- **ADR-029 (ревизия 2026-06-24 — ретирование `/v1/subscription/sync`, закрывает [Q-029-2](../99-open-questions.md)/[TD-021](../100-known-tech-debt.md)):** для изолированного сервиса claude-hermes StoreKit-путь подписок `POST /v1/subscription/sync` **ретируется** (роут/подписочная ветка кода удаляются), Adapty-вебхук остаётся единственным путём подписок. Риск двойного начисления (разные idempotency-ключи `sub-grant:*` vs `adapty-event:*`) устраняется by construction (второго пути нет), а не контрактной митигацией. `/v1/tokens/purchase` (consumable, [ADR-015](ADR-015-consumable-token-iap.md)) **сохраняется** (отдельный продукт, не подписка). Тело ADR-029 не переписано (immutability); пометка добавлена. См. [99-open-questions.md Q-029-2](../99-open-questions.md), [100-known-tech-debt.md TD-021](../100-known-tech-debt.md).
- **ADR-047 (расширение 2026-06-24 → [ADR-051](ADR-051-agent-debt-reconciliation.md)):** [ADR-051](ADR-051-agent-debt-reconciliation.md) расширяет §4/§6 ADR-047 — вводит реконсиляцию несписанной дельты (колонка `wallets.debt`, частичное списание остатка, clawback при пополнении, policy-блок `debt_outstanding`). Тело ADR-047 не переписано; §6 уточнено в ADR-051 §2.1 (полный savepoint-откат → расщепление debit/debt). Закрывает [Q-047-2](../99-open-questions.md) в части реконсиляции / [TD-029](../100-known-tech-debt.md).
- **ADR-048 (расширение 2026-06-24 → [ADR-052](ADR-052-durable-subscription-idempotency.md)):** [ADR-052](ADR-052-durable-subscription-idempotency.md) расширяет §2 ADR-048 — durable idempotency-якорь `subscription_grant_events` делает строгий `409` достижимым для **обоих** путей `grantCredits` (раньше `grantCredits=false` → later-writer-wins). Тело ADR-048 не переписано. Закрывает [TD-030](../100-known-tech-debt.md).
- **ADR-006 (ревизия 2026-06-23 → [ADR-047](ADR-047-usage-based-billing-for-agent.md)):** [ADR-047](ADR-047-usage-based-billing-for-agent.md) частично-суперсидит ADR-006 для контура `/v1/agent/*` ([ADR-045](ADR-045-hermes-as-agent-proxy.md)): биллинг агентного пути — **usage-based** (списание по реальному usage токенов, idempotency по `runId`), а не «1 кредит = 1 сообщение». Тело ADR-006 **не переписано** (immutability) и продолжает действовать для `/v1/chat/*` (1 кредит = 1 message-шаг) и для grant при подписке (§2). Пометка добавлена в шапку ADR-006.
- **ADR-037 (ревизия отображения 2026-06-19 → [ADR-042](ADR-042-hide-context-block-from-user-facing-history.md)):** settings-блок ADR-037 персистится в тексте user-шага (§4, для replay) и потому был виден в истории/превью. [ADR-042](ADR-042-hide-context-block-from-user-facing-history.md) срезает блок **только при отдаче** (history `_normalize_payload` + превью `_text_summary`, единый helper). Хранение (§4), инъекция в промт и реплей модели ADR-037 не изменены — модель по-прежнему получает блок. Пометка добавлена в шапку ADR-037 (immutability: тело не переписано).
- **ADR-017 (расширение 2026-06-10):** добавлен раздел «Мульти-инстанс / клонирование сервиса» — паттерн запуска нескольких изолированных инстансов одного кода claude-ios за общим edge-Traefik под разными доменами (первый `broadnova.shop`, второй `avelyraweb.shop`). Параметризация `docker-compose.prod.yml` через `COMPOSE_PROJECT_NAME` (дефолт `claude-ios`) для image-тегов и Traefik router/service-имён с **инвариантом обратной совместимости** (дефолт=текущее значение, `compose config` идентичен для существующего `.env`). Per-instance JWT keypair в `/opt/<inst>/.secrets/`, свежие секреты, INSTANCES-loop в CI. Само deploy-решение не изменено — добавлен playbook горизонтального клонирования. Playbook — [07-deployment.md §Мульти-инстанс](../07-deployment.md#мульти-инстанс--клонирование-сервиса). Связано с [Q-017-3](../99-open-questions.md).
- **ADR-003 (ревизия 2026-06-02):** в рамках MVP-решения зафиксирован `LocalKmsClient` как KMS-реализация для MVP (реальный AES-256-GCM wrap DEK под `KMS_LOCAL_MASTER_KEY`, тот же интерфейс `KmsClient`); миграция на облачный KMS — post-MVP ([Q-002-1](../99-open-questions.md)). Само решение envelope encryption не изменено — уточнена реализация на MVP. Пометка добавлена для трассируемости (запрос architect-reviewer).
- **ADR-044 (расширение 2026-07-06 → [ADR-058](ADR-058-x-user-id-string-identity.md)):** [ADR-058](ADR-058-x-user-id-string-identity.md) расширяет §1/§3 ADR-044 — `X-User-Id` принимает **произвольную непустую строку**: каноническая UUID-строка используется как есть (обратная совместимость), иная строка детерминированно маппится в `users.id` через `uuid.uuid5(HERMES_USER_NAMESPACE, …)`, пустой/whitespace → `401`. `users.id` остаётся UUID PK, миграций нет; модель доверия ([ADR-044 §3](ADR-044-client-api-key-auth.md)) и lazy provisioning ([ADR-007](ADR-007-lazy-user-provisioning.md)) не меняются. Тело ADR-044 не переписано.
- **ADR-044 (ревизия 2026-07-06 — ретирование HTTP-поверхности `/v1/auth/*`):** issuer-роутер `/v1/auth/*` (`src/app/api_gateway/routers/auth.py`) **НЕ регистрируется** в `create_app()` → маршруты отсутствуют, любой вызов `/v1/auth/*` → `404` (retired HTTP surface). Причина: без RS256-приватного ключа issuer отдавал `500`; клиентская идентичность — только `X-API-Key` + `X-User-Id` ([ADR-058](ADR-058-x-user-id-string-identity.md)). **Сохраняются** (спящий живой код, не retired): issuer/Apple-код `src/app/auth/*`, таблицы `auth_*` (миграции `0005`/`0012`), cleanup-reaper ([TD-013](../100-known-tech-debt.md)) и их unit/миграционные/reaper-тесты. **Ретайрены** только HTTP-поверхность и её интеграционные тесты (`test_auth_api.py`/`test_auth_apple_api.py`/`test_auth_unconfigured.py`). Расписано в [ADR-044 §4a](ADR-044-client-api-key-auth.md); согласовано с [ADR-059](ADR-059-hermes-only-openapi-surface.md) (тег `Auth` вне схемы). Тело §1–§4 ADR-044 не переписано.
